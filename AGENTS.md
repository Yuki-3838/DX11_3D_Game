# AGENTS.md

## 動画によるゲーム不具合調査（MP4添付時は自動実行）

- チャットにMP4動画が添付された場合、またはMP4動画のパスが指定された場合は、ユーザーが「解析して」と明示していなくても動画解析タスクとして扱う。
- MP4を検出したら、コード検索・コード調査・修正・原因の断定より先に、必ず `tools/analyze_video.py` でGemini APIへ動画を送り、解析結果を取得する。
- MP4が複数ある場合は、対象を明示されていなければ添付された全MP4を順番に解析する。
- 実行例: `python tools/analyze_video.py path/to/bug.mp4`
- `GEMINI_API_KEY` はWindowsの環境変数から取得する。APIキーをソースコード、設定ファイル、ログ、またはこのファイルへ書き込まない。
- 解析結果は既定で動画ファイルに `.analysis.md` を付けたMarkdownとして保存される。解析結果をCodexが読む場合は、そのファイルを確認する。
- Gemini解析が終わるまで、動画に関係するソースコードの調査や修正を開始しない。解析結果が保存されていることを確認してから、結果を手掛かりにソースコードと実行経路を調査する。
- Geminiの解析は調査の手掛かりであり、単独の根拠にしてコードを変更してはいけない。
- Geminiが述べた「観察した事実」と「推測」を区別し、推測は必ず実際のソースコード、実行経路、ログ、または再現結果と照合する。
- 原因がソースコードと照合できた場合のみ修正する。照合できない場合は、追加の観測・ログ・再現条件を求めるか、未確定の仮説として明示する。
- 動画解析に失敗した場合は、解析したことにせず、エラーを報告してから次の手順を相談する。
