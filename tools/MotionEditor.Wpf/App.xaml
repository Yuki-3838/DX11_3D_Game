<Application x:Class="DX11MotionEditor.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#20242B" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#3B4552" />
        <SolidColorBrush x:Key="AccentBrush" Color="#42B8E8" />
        <SolidColorBrush x:Key="SelectedBrush" Color="#F4C542" />
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3" />
            <Setter Property="Padding" Value="4,2" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="4" />
        </Style>
    </Application.Resources>
</Application>
