# 実装ログ

`docs/戦闘テンション設計_コンボ接続.md` のIssue A〜Jを、Codex(実装)とClaude(設計・レビュー・ビルド確認)で分担して進めた記録。
新しい作業をするたびに、下に追記していく。

---

## 2026-09-05

### Issue A + D — enemy移動バグ修正 / コンボyaw再設計
- **担当**: Codex(実装) / Claude(レビュー)
- **内容**:
  - [gameobject/enemy.cpp:155](../gameobject/enemy.cpp#L155) `m_srt.pos += m_move;` の二重加算を1回に修正。
  - [system/CCharacterAnimator.cpp:286](../system/CCharacterAnimator.cpp#L286) 弱攻撃torsoYawを `-0.34/+0.42/-0.10` → `-0.15/-0.30/-0.10`(片側漸増)に変更。
  - [system/CCharacterAnimator.cpp:366](../system/CCharacterAnimator.cpp#L366) 重攻撃torsoYawを `-0.12/+0.30/-0.20` → `-0.15/-0.35/-0.15` に変更。
- **確認**: 変更2ファイルのみ、clip/pitch/overheadは無改変であることを目視確認。

### Issue B + C — 敵の隙(Recovery)/予兆(Windup)をHUDで可視化
- **担当**: Codex(実装) / Claude(レビュー・ビルド確認)
- **内容**:
  - [scene/GameScene.cpp:1024](../scene/GameScene.cpp#L1024) `UpdateEnemyAnimation()`: Recovery突入後0.4秒、攻撃モーションの最終フレームで止め絵保持してからIdleへ切替。
  - [scene/GameScene.cpp:1302](../scene/GameScene.cpp#L1302) `DrawGameplayHud()`: ロックオンパネルがWindup中は赤橙枠+「INCOMING!」、Recovery中は緑枠+「CHANCE」に変化。ワールド空間のターゲット照準サークルの色も連動。
- **確認**: `scene/GameScene.cpp`のみ変更。Debug|x64でビルド成功(`x64\Debug\DX11_3D_Game.exe`生成、エラーなし)。

### デバッグモード切り替えの追加(Issueリスト外・追加要望)
- **担当**: Claude(実装)
- **内容**: [game.cpp](../game.cpp) に `dev_settings.ini` の `devmode=1` を読む仕組みを追加。有効時は起動時にTitleSceneを飛ばしGameSceneへ直行し、ImGuiデバッグ表示を最初から出す。ファイルが無ければ従来通り(提出用と同じ、タイトル開始・デバッグ非表示)。
- **状態**: ローカルの`dev_settings.ini`(未コミット)に`devmode=1`を設定済み。

---

### Issue J — モーションエディタにコンボ連続プレビュー機能を追加
- **担当**: Codex(実装) / Claude(レビュー・ビルド確認)
- **内容**:
  - [system/CCharacterAnimator.h:45](../system/CCharacterAnimator.h#L45) `StartComboPreview(bool heavy)` を追加。内部状態 `m_comboPreviewActive/Step/Heavy` を追加。
  - [system/CCharacterAnimator.cpp:388](../system/CCharacterAnimator.cpp#L388) `StartComboPreview()`実装。[811行目](../system/CCharacterAnimator.cpp#L811) `Update()`内で、現在の段のモーションが終わる(`!IsMotionPlaying()`)たびに次の段を自動再生し、3段目終了で停止。
  - [scene/MotionEditorScene.cpp:126](../scene/MotionEditorScene.cpp#L126) 「Preview Combo (Weak/Heavy)」ボタンを追加。
- **確認**: 指定3ファイルのみ変更。既存のGameScene側の通常コンボ呼び出しは無改変であることを確認。

### Issue E + F — コンボ段間ブレンドの可変化 / 攻撃中の腰の重心シフト
- **担当**: Codex(実装) / Claude(レビュー・ビルド確認)
- **内容**:
  - [system/CCharacterAnimator.h:193-194](../system/CCharacterAnimator.h#L193) `m_lastAttackTorsoYaw/Pitch` を追加(前回コンボ段の姿勢パラメータを保存)。
  - [system/CCharacterAnimator.cpp:1231-1234](../system/CCharacterAnimator.cpp#L1231) `ApplyAttackMotionDesign()`冒頭で、前回との`torsoYaw/torsoPitch`差分から`m_attackBlendDuration`を0.05〜0.15秒へ可変化。
  - [system/CCharacterAnimator.cpp:1245-1272](../system/CCharacterAnimator.cpp#L1245) 腰(`m_pelvis`)にキーが無い場合、既存の脊椎キー時刻(無ければ4点)を流用して腰キーを新規追加。
  - [system/CCharacterAnimator.cpp:1325-1328](../system/CCharacterAnimator.cpp#L1325) 腰へは`torsoPitch`の20%のみ(yawは加えない)を適用。脚のボーンは無改変。
  - [system/CCharacterAnimator.cpp:1372-1373](../system/CCharacterAnimator.cpp#L1372) 腰の回転を±0.12ラジアンにクランプ。
  - [system/CCharacterAnimator.cpp:1383-1384](../system/CCharacterAnimator.cpp#L1383) 末尾で今回のtorsoYaw/Pitchを保存(次回比較用)。
- **確認**: 指定2ファイルのみ変更。Debug|x64でビルド成功。
- **注意点(軽微)**: `m_pelvis`のクランプ(±0.12)は今回新規追加。もし将来「モーションファイル読み込み失敗時のフォールバックモーション」経路(`BuildFallbackAttackComboMotion`等、現状は資産が揃っているため通常は通らない)が使われた場合、そちらが元々持っていた大きめの腰の手付けモーション(0.3〜0.42ラジアン相当)もこのクランプで縮められる。現状のアセット構成では実害なし。

### 不具合修正 — デバッグUI表示中に回避・ダッシュが効かない
- **担当**: Claude(原因特定・修正)
- **症状**: 回避(Space)とダッシュ(LShift)が反応しない。攻撃(マウス)は効く。
- **原因**: [system/DebugUI.cpp:20](../system/DebugUI.cpp#L20) で `ImGuiConfigFlags_NavEnableKeyboard` を有効にしていたため、ImGuiウィンドウが表示されている間ずっと `io.WantCaptureKeyboard` が true になる。回避([scene/GameScene.cpp:790](../scene/GameScene.cpp#L790))とダッシュ([791行](../scene/GameScene.cpp#L791))は `!WantCaptureKeyboard` を条件にしているため常時ブロックされていた。攻撃は `WantCaptureMouse` 判定なので影響を受けない。
  - もともとF4でデバッグ表示をONにすると同じ症状が出る状態だったが、devmodeで起動時から表示するようにしたため常に発生するようになった。
- **修正**: `ImGuiConfigFlags_NavEnableKeyboard` を無効化(ImGuiのキーボードナビゲーションのみ無効。テキスト入力中は従来どおりWantCaptureKeyboardがtrueになるので、デバッグUIへの文字入力とゲーム操作は競合しない)。
- **確認**: Debug|x64でビルド成功。

### Issue G — 攻撃データを Combat::AttackData ベースへ一本化
- **担当**: Claude(実装)。※Codexはサンドボックスヘルパー(`codex-windows-sandbox-setup.exe`)のパス解決に失敗して着手できず、ファイルは一切変更されなかったため、こちらで実装した。
- **内容**:
  - 新規 [system/CombatAttackTable.h](../system/CombatAttackTable.h): 攻撃の時間・威力の唯一の定義元。`Combat::Tuning`名前空間の`inline constexpr`値(`static constexpr`が必要な既存コード用)と、`AttackData`を返すアクセサ(`PlayerWeakAttack()`/`PlayerHeavyAttack()`/`EnemyBasicAttack()`、今後の攻撃種別追加用)の2層構造。`AttackData`は`std::string`を含むためconstexpr化できず、この形にしている。ヘッダのみなのでvcxproj変更は不要。
  - [system/OneVsOneCombat.h](../system/OneVsOneCombat.h): ハードコードされていた`PLAYER_WINDUP`等の定数群を`Combat::Tuning`参照へ差し替え(定数名と`.cpp`側の使用箇所はそのまま=変更量を最小化)。
  - [system/OneVsOneCombat.cpp](../system/OneVsOneCombat.cpp): `ENEMY_HIT_TIMES`を`Combat::Tuning::ENEMY_FIRST_HIT_TIME`参照へ。
  - [gameobject/enemy.h](../gameobject/enemy.h): `WINDUP_SECONDS`/`ACTIVE_SECONDS`/`RECOVERY_SECONDS`を同じテーブル参照へ。
- **意図的な挙動変更(1点)**: 敵の予兆時間を **0.72秒→0.80秒に統一**。従来は敵AI・アニメ側が0.80秒、戦闘判定側が0.72秒とズレており、**予備動作アニメーションが終わる前に攻撃判定が始まっていた**。「予兆を見てから回避を決める」という本作の核に直接影響するため、長い方へ統一した。これ以外は数値・挙動を変えていない純粋なリファクタ。
- **確認**: Debug|x64でビルド成功。

### Issue H — 敵の攻撃パターンを3種類へ拡張
- **担当**: Claude(実装)
- **狙い**: 攻撃が1種類だと「待てば必ず同じことが起きる」ので読み合いが成立しない。予兆の長さ・射程・隙の大きさが異なる3種類を用意し、プレイヤーが予兆を見て「回避する/踏み込む/距離を取る」を選べるようにする。
- **追加した攻撃**([system/CombatAttackTable.h](../system/CombatAttackTable.h)):

| 攻撃 | 予兆 | 判定 | 硬直(隙) | 威力 | 射程 | 役割 |
|---|---|---|---|---|---|---|
| HEAVY SLAM(叩き付け) | 0.80s | 0.90s | 1.35s | 15 | 64 | 標準。予兆も隙も大きい |
| QUICK BITE(噛みつき) | 0.42s | 0.55s | 0.70s | 10 | 48 | 速い。様子見しすぎると刺される。隙が小さいので欲張った反撃は危険 |
| WIDE SWEEP(薙ぎ払い) | 1.10s | 0.85s | 1.80s | 22 | 78 | 遅いが射程が広く距離を取るだけでは避けにくい。隙が最大で大きく反撃できる |

- **内容**:
  - [system/CombatAttackTable.h](../system/CombatAttackTable.h): `EnemyAttackKind`(Slam/Bite/Sweep)と各AttackData、種類→データ/初撃タイミング/HUD表示名を引く関数を追加。
  - [gameobject/enemy.h](../gameobject/enemy.h) / [enemy.cpp](../gameobject/enemy.cpp): 攻撃の時間を`static constexpr`から**選択中の攻撃データを参照する関数**(`windupSeconds()`等)へ変更。Windup遷移時に`selectNextAttack(distance)`で攻撃を選ぶ。噛みつきは射程内のときだけ候補に入れ、直前と同じ攻撃が連続しないようにしている(完全ランダムだと読む意味が薄れ、順番固定だと暗記ゲームになるため中間を取った)。薙ぎ払いはActive中の踏み込みも大きくして、射程の広さが動きから読めるようにした。
  - [system/OneVsOneCombat.h](../system/OneVsOneCombat.h) / [.cpp](../system/OneVsOneCombat.cpp): `SetEnemyAttackKind()`を追加。敵の攻撃の時間・威力・射程・クールダウンを選択中の攻撃データから引くよう変更(従来は固定定数)。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): 毎フレーム敵の選択した攻撃を戦闘システムへ渡す。HUDの予兆表示を「INCOMING!」固定から**攻撃名(HEAVY SLAM / QUICK BITE / WIDE SWEEP)**に変更。さらに敵のモーションクリップが1本しか無いため、**攻撃の種類に応じて再生速度を変更**(予兆が短い噛みつきは速く、長い薙ぎ払いは遅く)し、アセット追加なしで見た目にも差が出るようにした。
- **確認**: Debug|x64でビルド成功。
- **未確認(要プレイテスト)**: 3種類の出現バランス、各攻撃の威力・射程の妥当性、再生速度変更が不自然に見えないか。数値は全て`CombatAttackTable.h`の`Tuning`名前空間にあるので、そこだけ触れば調整できる。

### スキニングをLBS単独からLBS/DQSハイブリッドへ変更
- **担当**: Claude(実装・ドキュメント)
- **背景**: 攻撃モーションで上半身を大きく捻ると頂点が潰れる(キャンディラッパー現象)ため、外部モーションを全身へ適用できず、腕のチェーンだけに制限していた。これがモーション表現の上限を決めていた。
- **内容**:
  - 新規 [shader/vertexLightingOneSkinVSHybrid.hlsl](../shader/vertexLightingOneSkinVSHybrid.hlsl): LBSとDQSを頂点シェーダ内で両方計算し`DQS_BLEND`(既定0.75)で混合。定数バッファは従来のまま(4x4行列)で、**シェーダ内で行列→デュアルクォータニオンへ変換**しているため、CPU側・頂点フォーマット・モデルアセットは無変更。反対称性(antipodality)対策、ボーンインデックス範囲チェック、ウェイト合計補正も実装。
  - [scene/GameScene.cpp](../scene/GameScene.cpp) / [scene/MotionEditorScene.cpp](../scene/MotionEditorScene.cpp): 登録するスキンシェーダをハイブリッド版へ差し替え。従来のLBS版(`vertexLightingOneSkinVSSafe.hlsl`)は比較・ロールバック用に残してある。TitleSceneは従来のままなので、両者を見比べられる。
  - 技術解説を [docs/スキニング技術解説.md](./スキニング技術解説.md) に作成(就活資料用。採用理由・他手法との比較・限界を記載)。
- **確認**: Debug|x64でビルド成功。HLSLは実行時コンパイルのため、`fxc /T vs_5_0`で新旧両シェーダの構文検証も実施(いずれも成功)。
- **未確認(要目視)**: 実際の描画結果。特に曲げ関節(肘・膝)の膨らみが目立たないか、`DQS_BLEND`の値が適切か。値はシェーダ冒頭の定数1つで調整できる。

### プレイヤー攻撃モーションを全身へ適用 / コンボ段ごとに別クリップへ
- **担当**: Claude(実装)
- **背景**: プレイヤーの攻撃は「胴・脚が静止したまま腕だけが振られている」状態だった。加えて3段コンボが全段とも同じFBXクリップを再生していた。
- **調査で判明した重要な事実**:
  - 腕だけに制限していた理由(コード内コメント)は「元モーションの胴体キーで全身が折れる」というもの。しかしこれは**当時のプレイヤーがMMD系リグだった頃の制約**で、現在のプレイヤーは`assets/model/SwordShieldPack/runtime/SwordShieldPack_Player.glb`(ボーン名が`mixamorig:`)であり、攻撃クリップも同じSword and Shield Pack由来。**同一リグなのでリターゲット不一致は起きない**。
  - [system/CAnimationMesh.cpp:986-999](../system/CAnimationMesh.cpp#L986) で、クリップから取り込むのは**回転のみ、平行移動は常にレスト姿勢**。そのため下半身を含めてもルート移動の二重適用は起きない。
- **内容**:
  - [system/CCharacterAnimator.cpp:162](../system/CCharacterAnimator.cpp#L162)付近: 取り込みボーンを「腕チェーンのみ」から「**ルート・腰・指・剣ボーンを除く全身**」へ変更。背骨・首・頭・脚・足が動くようになり、踏み込みと重心移動が出る。
  - [scene/GameScene.cpp:1501](../scene/GameScene.cpp#L1501)付近: 弱攻撃3段を `slash (5)/(2)/(3)`、強攻撃3段を `attack (4)/(2)/(3)` へ変更(1段目は従来の動作確認済みクリップを維持)。参照する6ファイルの実在を確認済み。
- **確認**: Debug|x64でビルド成功。参照クリップの存在確認済み。
- **未確認(要目視)**: 全身適用後の見た目。特に(1)脚が地面から浮かないか、(2)コンボ2段目・3段目のクリップが1段目と繋がって見えるか。モーションエディタ(F3)の「Preview Combo (Weak/Heavy)」で3段続けて確認できる。
- **次の調整候補**: 腰(hips)は今回まだ除外している。クリップ中の腰の回転は大きく、ロックオン中に正面がズレて見える恐れがあるため段階的に進める方針。全身適用が問題なければ腰も加えると体重移動がさらに出る(`isGameplayRoot`の条件から腰を外す)。

### アニメーションのキーフレーム補間(slerp)を実装 ← モーションが「カクつく」根本原因
- **担当**: Claude(原因特定・実装)
- **判明した欠陥**: **キーフレーム補間がどこにも実装されていなかった**。
  - [system/CAnimationMesh.cpp:972](../system/CAnimationMesh.cpp#L972)付近(プレイヤー): `std::lround`で最も近いキー番号へ丸め、そのキーの値をそのまま使用。
  - [system/CAnimationMesh.cpp:848](../system/CAnimationMesh.cpp#L848)付近(敵): `CurrentFrame % mNumRotationKeys` の整数インデックス。コメントに`//簡易実装`と明記されている。
  - 元クリップは約30fpsで焼かれており、ゲームは65Hzで描画される。補間が無いと同じ姿勢が数フレーム続いてから次のキーへ飛ぶため、**全アニメーションが段階的にスナップして見える**。元のモーションの品質に関係なく破綻する。
  - これまでのIssue D/E/F(yaw調整・可変ブレンド・腰の前傾)や全身適用が効果を感じられなかった主因はここにあった可能性が高い。
- **内容**:
  - [system/CAnimationMesh.h](../system/CAnimationMesh.h) / [.cpp](../system/CAnimationMesh.cpp): `UpdateAnimationWithManualPose()`に`frameFraction`(キー番号の小数部、既定0.0)を追加。隣接キー間を`aiQuaternion::Interpolate`(最短経路slerp、反対称性の処理込み)で補間するよう変更。既定値があるため既存の呼び出しは変更不要。
  - [system/CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp): 攻撃クリップと移動クリップの再生で、フレーム累積値の小数部を`frameFraction`として渡すよう変更(従来は`static_cast<int>`で切り捨てていた)。
- **確認**: Debug|x64でビルド成功。
- **未対応**: 敵側の`BuildLocalPoseMap()`(`Update()`経由)はまだ補間していない。こちらは呼び出し側が整数フレームしか持っていないため、小数フレームを渡す改修が別途必要。敵の動きのカクつきが気になる場合は次に対応する。

### 画面キャプチャによる実機確認と、全身適用の差し戻し(脚のみ)
- **担当**: Claude
- **経緯**: それまでコードだけで判断しており、実際の描画を一度も見ていなかった。PowerShellでゲームを起動し、ウィンドウを前面化して`CopyFromScreen`で連続キャプチャする手順を用意して、初めて実機の見た目を確認した。
  - 手順スクリプト(参考): ウィンドウはタイトルではなくプロセス内で最大のクライアント領域で特定する(ImGuiのビューポートが別ウィンドウとして複数存在するため)。`SetForegroundWindow`は単体では効かず、`AttachThreadInput`で対象スレッドへアタッチしてから呼ぶ必要がある。
- **判明した不具合**: 直前に入れた「攻撃モーションの全身適用」で、**キャラクターが座り込んだように脚が開いて崩れていた**。
  - 原因: 脚のボーンだけをクリップで動かし、腰(hips)を固定していたため。Mixamoのクリップの脚の回転は腰も一緒に回転・上下することを前提にしており、腰が沈まないまま脚だけが「沈んだ姿勢用の角度」を取ると、脚が横へ開く。
  - `CAnimationMesh`は平行移動を常にレスト姿勢へ固定する実装なので、腰を含めてもこの問題は解決しない(腰の平行移動を扱えるようにする改修が別途必要)。
- **対応**: [system/CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp)の取り込みボーン条件に脚の除外(`leg`/`thigh`/`knee`/`calf`/`foot`/`ankle`/`toe`)を追加。体幹(背骨・首・頭)と腕は取り込んだまま。再ビルドし、再キャプチャして崩れが解消したことを目視確認済み。
- **実機を見て分かった、モーション以外の品質要因**(いずれも未対応):
  1. **脚が完全に静止している** — 上記の理由で脚を除外しているため。踏み込みが無く「棒立ちで腕を振る」印象になる。根本解決には腰の平行移動対応が必要。
  2. **キャラクターが画面に対して小さい** — 720pの画面で全高200px程度。`ThirdPersonCamera`の`m_lookDistance = 70` / `m_targetHeight = 25`。攻撃時にカメラを寄せるだけでも印象は大きく変わる。
  3. **影が無い** — 接地感が無く、キャラも敵も地面に浮いて見える。
  4. **エフェクトが一切無い** — 剣の軌跡、ヒット時のフラッシュ、土煙など。モンハン系の手応えはVFX・ヒットストップ・カメラシェイクで作られている部分が大きい。
  5. **ライティングが平坦** — 環境は無地の茶色、鎧と剣にハイライトが乗らない。

### 攻撃中のカメラ寄せ / 接地影の追加
- **担当**: Claude(実装・実機キャプチャで確認)
- **狙い**: 実機キャプチャで判明した「キャラクターが画面に対して小さい」「影が無く地面から浮いて見える」を解消する。モーションそのものより先に、見た目の印象を決めているこの2つに手を入れる。
- **内容**:
  - [scene/GameScene.cpp](../scene/GameScene.cpp) `UpdateCombatCameraDistance()`(新規): 攻撃中はカメラ距離を基準の0.62倍へ寄せ、終了後は緩やかに戻す。指数的な補間で酔いを防ぐ。寄る速さ(7.0)より戻る速さ(3.2)を遅くして、余韻を残す。
    - 呼び出し順は「攻撃の寄せ → 壁への寄せ(`UpdateArenaCameraCollision`) → カメラ更新」。`SetCollisionDistance`は基準距離を上限にクランプするため、先に基準距離を確定させる必要がある。
    - 基準距離・基準注視点は`init()`で`ThirdPersonCamera`の既定値から取得(値の二重管理を避けるため)。
  - [scene/GameScene.cpp](../scene/GameScene.cpp) `DrawGroundShadows()`(新規): プレイヤーと敵の足元へ、暗い扁平な円柱(`Cylinder`)を接地影として描画。半径はモデルの実頂点から作った境界(`m_localPlayerMeshBounds`/`m_localEnemyMeshBounds`)から算出するため、モデルの倍率を変えても影の大きさがズレない。闘技場の床(上面 約Y=-0.29)のわずかに上へ置いてZファイティングを回避。
- **確認**: ビルド後、実機を起動してキャプチャし、寄せと影の両方が意図どおり効いていることを目視確認済み。
- **試して取りやめたこと**: 「寄せるとキャラクターが画面下寄りになる」ため注視点(`m_targetHeight`)も下げてみたが、実機で確認すると地面寄りを見る構図になり、接近した敵に視界を塞がれてかえって見づらくなったため取りやめた。距離だけを変えている。
- **残る課題**: 敵が密着するとカメラの視界を塞ぐ(カメラの衝突判定は壁のみが対象で、敵は考慮していない)。

### シャドウマッピングの実装(簡易接地影からの置き換え)
- **担当**: Claude(実装・実機キャプチャで確認)
- **狙い**: 足元に暗い円盤を敷くだけの簡易接地影を、平行光源による本物の影に置き換える。キャラクターや敵の形がそのまま影になる。
- **構成**: ライトから見た深度を書き込むパス → 本描画で各ピクセルをライト空間へ変換して深度比較、の2パス構成。
- **内容**:
  - [system/renderer.h](../system/renderer.h) / [.cpp](../system/renderer.cpp): 2048x2048の深度テクスチャ(`R32_TYPELESS`)を作り、深度ステンシルビュー(`D32_FLOAT`)とシェーダーリソースビュー(`R32_FLOAT`)を別フォーマットで張る。`BeginShadowPass()`/`EndShadowPass()`/`SetLightViewProjection()`を追加。比較サンプラー(`COMPARISON_MIN_MAG_LINEAR_MIP_POINT`)をs1へ、影用の定数バッファをb7へ割り当て。
  - [shader/common.hlsl](../shader/common.hlsl): b7に`LightViewProjection`と影の設定、t1に影テクスチャ、s1に比較サンプラーを追加。`PS_IN`へ`ShadowCoord`を追加し、`CalcShadowCoord()`と3x3 PCFの`CalcShadowFactor()`を用意。
  - 影生成用に [shader/shadowDepthVS.hlsl](../shader/shadowDepthVS.hlsl)(スキンなし)、[shadowDepthSkinVS.hlsl](../shader/shadowDepthSkinVS.hlsl)(スキンあり)、[shadowDepthPS.hlsl](../shader/shadowDepthPS.hlsl) を新規作成。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): `BuildLightViewProjection()`(ライトの向きから正射影を作る。範囲はプレイヤー中心に絞り、限られた解像度を必要な場所へ集中させる)と`RenderShadowMap()`を追加し、`draw()`の先頭で実行。簡易接地影(`DrawGroundShadows`)は削除。
- **実装上のポイント**:
  - 影テクスチャがピクセルシェーダーへ割り当てられたままだと書き込み先にできないため、`BeginShadowPass()`で先にt1を外している。
  - 影生成時だけ深度バイアス付きのラスタライザへ切り替え、シャドウアクネ(自分自身を影と誤判定する縞模様)を防いでいる。`EndShadowPass()`で通常状態へ戻す。
  - シャドウマップの範囲外は「影なし」として扱うため、比較サンプラーの境界色を1.0にしてクランプしている。
- **踏んだ不具合**: `PS_IN`へ要素を追加した結果、[shader/GeometryShader.hlsl](../shader/GeometryShader.hlsl)が`variable 'output' used without having been completely initialized`でコンパイルに失敗し、**起動時にエラーダイアログが出て画面が真っ黒**になった。
  - 注意: 実行時コンパイルは`D3DCOMPILE_ENABLE_STRICTNESS`付き。`fxc`で検証する際も`/Ges`を付けないと再現しない。
  - `PS_IN`に要素を追加したら、それを出力する**全ての頂点シェーダーとジオメトリシェーダー**で値を設定する必要がある。
- **確認**: ビルド後、実機を起動してキャプチャし、ドラゴンの翼・脚やプレイヤーの形が影として落ちていることを目視確認済み。

### 攻撃クリップを dev_settings.ini から差し替え可能にした / 全候補の撮り比べ
- **担当**: Claude
- **狙い**: `assets/motion` には攻撃系だけで9本(attack×4, slash×5)あるのに6本しか使っておらず、どれが良いかは実際に動かして見ないと判断できない。ビルドし直さずに比較できるようにする。
- **内容**:
  - [scene/GameScene.cpp](../scene/GameScene.cpp): 無名名前空間に`GetDevSetting(key, fallback)`を追加し、コンボ各段のクリップパスを`dev_settings.ini`から読むようにした(キー: `weak1`〜`weak3`, `heavy1`〜`heavy3`)。指定が無ければ従来の既定クリップを使うため、提出時の挙動は変わらない。
  - 9本すべてについて、iniを書き換え→起動→攻撃→キャプチャ→終了 を自動で回して撮り比べた(1本あたり4フレーム)。
- **分かったこと**:
  - `slash.fbx` と `slash (4).fbx` はシルエットが大きく、剣の位置が読み取りやすい。
  - `attack.fbx` / `attack (2).fbx` は腕を体に寄せた小さめの動きで、迫力は出にくい。
  - ただし**どのクリップを選んでも脚は動かない**(腰を固定している構造上の制約)。クリップ選定では解決しない。
- **副次的な効果**: 新しくFBXを`assets/motion`へ置いた場合も、iniでパスを指すだけで試せるようになった(コード変更・再ビルド不要)。

### 待機モーションの追加
- **担当**: Claude(実装・実機キャプチャで確認)
- **背景**: 待機は静止姿勢へ背骨のわずかな呼吸を手続き的に加えるだけで([CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp)の`idleBreath`)、腕を垂らした棒立ちのA字ポーズに見えていた。`assets/motion`にidleクリップが4本あるのに未使用だった。
- **内容**:
  - [system/CCharacterAnimator.h](../system/CCharacterAnimator.h) / [.cpp](../system/CCharacterAnimator.cpp): `SetIdleAnimation()`を追加。`Update()`で「歩行中でもジャンプ中でも攻撃中でもない」ときに待機クリップをループ再生する(キー間はslerp補間、再生速度は0.5キー/更新で元の30fps相当)。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): idleクリップを読み込んでアニメーターへ設定。クリップは`dev_settings.ini`の`idle`キーで差し替え可能。
- **ここでも脚は除外した**: 最初は移動クリップと同じ範囲(脚を含む)で実装したが、実機で確認すると**攻撃モーションのときと全く同じ「脚が開いて座り込む」破綻**が出た。待機クリップも腰が上下する前提で作られているため、腰を固定したまま脚だけ動かすと沈んだ姿勢用の角度になる。体幹(背骨)と腕のみに絞って解決。
- **確認**: idleクリップ4本すべてを撮り比べ。脚を除外した状態では4本の差は小さい。既定は`sword and shield idle.fbx`。
- **正直な評価**: 静止していた待機に呼吸と体の揺れが入ったが、**脚を適用できないため改善幅は限定的**。

### アニメーションのレイヤー合成(上半身・下半身の分離)と腰の沈み込み対応 ← 脚の問題を解決
- **担当**: Claude(実装・実機キャプチャで確認)
- **きっかけ**: 外部からのアドバイスで「ブレンドツリーを実装すれば歩きながら攻撃できる」と指摘を受けた。検討したところ、**これは同時に「攻撃中に脚が静止する」問題も解決する**と分かった。腰の平行移動を直す前に、下半身を移動・待機クリップに任せてしまえばよい。
- **内容**:
  - [system/CAnimationMesh.h](../system/CAnimationMesh.h) / [.cpp](../system/CAnimationMesh.cpp):
    - 既存の適用処理を`ApplyAnimationToBones()`として切り出し(休止姿勢へのリセットは呼び出し側が行うので、複数のクリップを重ねられる)。
    - `UpdateLayeredAnimation()`を追加。ベース(下半身)とオーバーレイ(上半身)の2本を、それぞれのボーン一覧へ適用して合成する。
  - [system/CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp): 攻撃・待機・移動のすべてをレイヤー合成の経路へ統一。下半身は移動または待機クリップ、上半身は攻撃クリップが担当する。下半身のボーン一覧は`LowerBodyBones()`に集約。
- **腰の沈み込み(平行移動)対応**: レイヤー分けだけでは脚が横に開く問題が残ったため、腰に限り**平行移動のみ**をクリップから取り込むようにした(`ApplyAnimationToBones`の`translationBone`引数)。
  - **絶対座標ではなくクリップ先頭からの相対オフセット**として休止姿勢へ足す。ワールド位置はゲーム側(SRT)が管理しているため、絶対座標だと二重移動になる。
  - **回転は取り込まない**。一度は回転も含めたところ、**キャラクター全体が傾いて寝転んだような姿勢**になった(実機キャプチャで確認)。腰の回転はキャラクターの向きそのもので、クリップ側の基準姿勢とモデルの休止姿勢が一致しないため。欲しいのは沈み込みだけなので回転は休止姿勢のまま維持する。
- **結果**: 攻撃中も待機中も脚が構えを保つようになった。「歩きながら攻撃」もこの構造で表現できる。
- **確認**: 実機キャプチャで、待機・攻撃の両方で脚が破綻せず構えを保っていることを目視確認済み。

### 不具合: 敵(ドラゴン)が地面から浮いている(修正済み)
- **担当**: Claude(原因特定・修正)
- **症状**: 敵が地面から浮いて見える。足と影の間に隙間がある。
- **原因**: **接地オフセットの計算に使う軸が間違っていた**。敵は[gameobject/enemy.cpp:66](../gameobject/enemy.cpp#L66)の`getRenderSRT()`で、描画時にX軸へ+90度回転させている(ドラゴンのモデルが傾いた姿勢で作られているため)。この回転ではローカルの+Zがワールドの-Yを向くため、**一番低い点はローカルAABBの`min.y`ではなく`max.z`で決まる**。ところが[scene/GameScene.cpp](../scene/GameScene.cpp)ではプレイヤーと同じ`min.y`で計算しており、別の軸の寸法を使っていた。
- **修正**: `enemyGroundY - min.y * scale` → `enemyGroundY + max.z * scale`。
- **確認**: 足元を拡大したキャプチャで、修正前後を比較。前脚が地面に着き、影が足元から連続していることを確認済み。
- **注意**: プレイヤー側は描画時の回転補正が無いため`min.y`のままで正しい。同じ式を使い回さないこと。

#### 不具合: 待機中に宙へ浮いて脚をばたつかせる(修正済み・腰の沈み込みは取り下げ)
- **症状**: 待機中、キャラクターが浮いたように見え、脚が横へ跳ね上がってばたついていた。
- **原因**: 待機クリップの脚は**腰が大きく沈む前提**で作られている。腰を固定したまま脚の回転だけを適用すると、脚が空中で開いた姿勢になる。埋まり対策で沈み込みを腰の高さの±12%に制限したことで、脚の角度に見合う沈み込みが全く足りていなかった。
- **判断**: 腰の沈み込みを平行移動で再現する方針そのものを取り下げた。正しくやるには**脚のIKで接地を保つ仕組み**が必要で、規模が大きい。
- **修正後の構成**:
  - 立ち止まっている間は脚を動かさない(待機・攻撃とも休止姿勢)。両者で同じなので、攻撃に入る瞬間に脚が飛ぶこともない。
  - 歩行・走行中は従来どおり移動クリップが脚を動かす(こちらは腰の上下が小さく破綻しない)。
  - **移動中の攻撃はレイヤー合成が有効**。下半身は移動クリップ、上半身は攻撃クリップ。「歩きながら攻撃」はこの構成で動く。
  - 腰(pelvis)はどの経路でも動かさない。`ApplyAnimationToBones`の`translationBone`は残してあるが、現在は誰も指定していない。
- **確認**: 足元を拡大したキャプチャで、脚が接地して静止していること、影が足元から伸びていることを確認済み。

#### 不具合: プレイヤーが地面へ埋まっていく(修正済み)
- **原因**: 腰の沈み込みを実装した際、**クリップとモデルで長さの単位が違う**ことを考慮していなかった。FBXはcm単位で作られていることが多く、キー間の差分をそのままモデルのローカル座標へ足すと桁違いに大きな値になる。結果、腰が大きく下がってキャラクターが地面へ埋まっていた。
- **修正**: 差分をそのまま足すのをやめ、**「クリップ内での腰の高さに対する割合」へ変換してからモデル側の腰の高さへ掛ける**方式にした。これで単位に依存しない。さらに沈み込みは腰の高さの±12%までにクランプしている。
- **併せて修正**: 前後左右の平行移動は取り込まないようにした(上下のみ)。ワールド移動はゲーム側が管理しているため、取り込むと足が滑ったりその場でずれていく。
- **確認**: 時間差で撮影した複数フレームで、足が接地したまま沈み込みが進行しないことを確認済み。

## 以前のボトルネック(解決済み)

**腰(hips)の平行移動を扱えないこと**が、攻撃・待機の両方で脚を使えない原因になっている。
同じ根本原因で2回破綻しており、ここを直せば両方まとめて解決する。
- 現状: [system/CAnimationMesh.cpp:986](../system/CAnimationMesh.cpp#L986)付近で、クリップからは回転のみを読み、平行移動は常にレスト姿勢へ固定している。
- 方針: 攻撃・待機クリップに限り、腰の平行移動を「レスト姿勢からの相対オフセット」として取り込む。ワールド位置はゲーム側(SRT)が管理しているため、絶対座標をそのまま入れると二重移動になる。

## 2026-09-09 敵アニメーションのキー間補間と敵の埋まり修正

- 担当: Codex。`system/CAnimationMesh.h/.cpp`と`scene/GameScene.cpp`を変更。
- 約10更新に1度の姿勢更新を毎固定更新へ変更。回転はslerpで補間する。
  敵のワールド位置と接地は`GameScene`側のSRTで管理しているため、位置キーは補間せず従来どおり現在キーを使う。
- 位置キーまで線形補間した初版では、ルートを含むアニメーションの上下移動が描画SRTへ重なり、敵が地面へ埋まる症状が出た。
  位置補間を不採用に戻し、敵の上下位置をアニメーション補間が変更しないように修正した。
- 既存のキー送り速度と攻撃種別の速度差を維持。Recoveryは補間を止め、各チャンネルの末尾へクランプする。
- 既存呼び出しとの互換性のため小数部0・ループありを既定引数とした。初回ビルドでブレンダーからの呼び出しに既定引数が必要と判明し修正。
- 検証: Debug|x64ビルド成功(終了コード0)、差分の空白検査成功、ゲーム起動・応答確認。本画面を取得できず動きの目視確認は未完了。
- 制約: キー時刻ベースの再生ではなく既存キー番号ベースの回転補間。位置キーの中間値や、死亡クリップは対象外。

### 2026-09-09 敵スキニング後の接地補正

- `system/CAnimationMesh.h/.cpp`に`GetAnimatedLocalMaxZ()`を追加し、現在のボーンコンビネーションでスキニングした全頂点の最上位ローカルZを計算するようにした。
- `scene/GameScene.cpp`では、その値を敵の描画専用接地オフセットへ反映した。敵の物理SRTや衝突位置は変更せず、描画された足元だけがフィールドの高さより下へ入らないようにした。
- 原因: 静的メッシュのAABBだけで接地位置を決めていたため、アニメーションで脚・胴が変形したときの最下点を拾えていなかった。
- 採用理由・利点: モーションごとに固定値を増やさず、現在姿勢の実頂点から補正できる。追加した敵にも同じ補正を適用する。
- 制約: CPU側の接地推定はLBSで計算し、描画側のLBS/DQSハイブリッドとの差を安全余白で吸収する。頂点数に比例するCPU計算が固定更新ごとに発生する。
- 検証: Debug|x64ビルド成功(警告0・エラー0)、修正版起動後のプロセス応答を確認。デバッグ用別ウィンドウが前面に出るため、足元の完全な目視確認は未完了。

### 2026-09-09 敵カットシーンから戦闘への影ずれ・後ろ足の姿勢ずれ修正

- **症状**: 敵の登場カットシーンから戦闘へ切り替わる付近で、本体の脚の輪郭と影が一致しない。後ろ足が前の姿勢を引きずったように見える可能性があった。
- **原因1**: 本体描画はLBS/DQSハイブリッドだったのに、影生成用`shadowDepthSkinVS.hlsl`はLBSのみだった。同じボーン行列でも、DQSで変形した本体とLBSで作った影の輪郭が異なるため、脚のような接地部でずれが見えやすかった。
- **原因2**: カットシーン中のIdleアニメーションで進んだフレーム番号を、戦闘開始後のWalkへリセットしていなかった。遷移直後だけ、前のクリップのフレーム位置から再生される状態だった。
- **修正**:
  - [shader/skinTransform.hlsli](../shader/skinTransform.hlsli)へLBS/DQSハイブリッドの頂点位置計算を集約し、本体と影の両方から共有するようにした。
  - [scene/GameScene.cpp](../scene/GameScene.cpp)で戦闘開始時に敵のアニメーションフレームとtickを0へ戻し、Walkを先頭から開始するようにした。
- **採用理由・利点**: 影側だけLBSへ戻すより、本体と影の頂点位置を同じ関数で計算する方が見た目の整合性を直接保証できる。共有includeにしたため、今後DQS比率や安全処理を変更しても片方だけ更新する不整合を防げる。アニメーションのリセットは遷移時だけの小さな状態変更で、SRT・衝突・物理位置には影響しない。
- **不採用案・欠点**: 影だけDQSを無効化して本体をLBSへ揃える案は実装が小さいが、既に導入したDQSのシルエット改善を本体側から失うため不採用とした。別々のシェーダーへ同じ計算を複製する案は動くが、将来の修正漏れを招くため共有includeを採用した。DQS計算を影にも行う分、影頂点シェーダーの計算量はLBS単独より増える。
- **限界**: 法線は従来どおりボーン変形を反映していないため、影の輪郭が一致しても陰影の向きは別課題。CPU側の接地推定もLBSのままなので、描画側DQSとの差を完全にゼロにはしていない。
- **検証**: Debug|x64ビルド成功(警告0・エラー0)。実行ファイルの起動・プロセス応答を確認し、`R`でリセット後に戦闘状態へ遷移することを確認した。HLSLは実行時コンパイルのため、起動確認まで実施した。デバッグUIが前面に出る環境では後ろ足の全フレームを連続目視できていないため、影と後ろ足の最終的な見た目確認は継続する。

### 2026-09-09 敵カットシーン→戦闘の回転クロスフェード

- **症状**: IdleのカットシーンからWalkへ切り替えるとき、フレームを0へ戻すだけでは後ろ足の姿勢が一瞬跳ねる可能性があった。
- **修正**: [system/CAnimationMesh.h/.cpp](../system/CAnimationMesh.h)に`UpdateBlendedRotationAnimation()`を追加し、カットシーン直前のIdle姿勢とWalkの先頭姿勢を0.18秒だけ回転slerpするようにした。[scene/GameScene.cpp](../scene/GameScene.cpp)では、遷移直前に実際に表示したIdleのフレームと小数部を保存してブレンド元に使う。
- **位置を補間しない理由**: 敵のワールド移動・接地はSRTと描画接地補正が管理している。位置キーまで混ぜると、以前発生したルート移動の二重適用による埋まりが再発するため、位置とスケールはWalk側をそのまま使う。
- **採用理由・利点**: ハードリセットより姿勢の段差を抑えられ、既存のSRT・衝突・影のボーン行列を変更しない。影パスは本体と同じボーン行列を使うため、ブレンド中も本体と影の変形が一致する。
- **不採用案・欠点**: フレーム0への即時切り替えは実装が簡単だが姿勢のスナップが残る。位置も含めた完全なポーズ補間は見た目は滑らかだが、ルート移動・接地と競合して埋まりやすい。今回の回転のみ方式は、Walk先頭の足位置そのものがIdle終端と異なる場合の微小な足滑りを完全には解決しない。
- **検証**: Debug|x64ビルド成功(警告0・エラー0)、ゲーム起動・プロセス応答確認、起動後に戦闘状態へ進むことを確認。デバッグ小窓のキャプチャ制約により、0.18秒間の後ろ足と影の連続フレームは未完了。

### 2026-09-09 壁がシャドウを受けない不具合修正

- **症状**: キャラクターの影は床へ出るが、闘技場の壁には影が適用されなかった。
- **原因**: 壁は面の明暗を一定にするため`Box::DrawUnlit()`を使っており、`unlitTexturePS.hlsl`では`CalcShadowFactor()`を呼んでいなかった。影を生成する側の`RenderShadowMap()`から壁を除外していること自体は正しいが、壁を受け手として描くピクセルシェーダー側の処理が不足していた。
- **修正**: `unlitTexturePS.hlsl`でも`In.ShadowCoord`を使って`CalcShadowFactor()`を適用した。これにより、無照明の壁・床は面の向きによる明暗を持たないまま、キャラクターの影だけを受ける。
- **採用理由・利点**: 壁専用の描画経路を新設せず、既に`unlitTextureVS.hlsl`が出力しているライト空間座標をそのまま使える。壁だけでなく同じ無照明シェーダーを使う床や追加の箱にも自動で適用される。
- **不採用案・欠点**: 壁を影生成パスへ追加する案は、壁自身を影の投射物として深度へ書くため不要で、シャドウアクネや範囲の問題を増やす。壁専用ピクセルシェーダーを作る案は影対応を複数箇所へ分けて保守漏れが起きやすいため不採用とした。無照明オブジェクト全体が影を受けるため、別シーンでこのシェーダーを使う場合は影の有効フラグを正しく設定する必要がある。
- **検証**: `fxc /Ges`で`unlitTexturePS.hlsl`を検証し、Debug|x64ビルド成功、ゲーム起動・プロセス応答を確認する。

### 2026-09-10 Codex実装分の実機目視確認(Claude)

Codexが「デバッグ用の別ウィンドウが前面に出る」「描画ウィンドウを取得できない」として
未完了にしていた目視確認を、キャプチャ手順で実施した。

- **キャプチャができなかった原因と対処**: ImGuiのビューポート機能でデバッグ窓が別のOSウィンドウになるため、
  `MainWindowHandle`では小さなImGui窓を掴んでしまう。**プロセス内で最大のクライアント領域を持つウィンドウ**を
  選ぶことで本体を特定できる。さらに`SetForegroundWindow`は単体では効かないので、
  `AttachThreadInput`で対象スレッドへアタッチしてから呼ぶ必要がある。
  (手順の詳細は`docs/Codex引き継ぎメモ.md`の「実機の見た目を確認する手順」を参照)
- **確認できたこと**:
  - Debug|x64ビルド: エラー0・警告0(終了コード0)。MotionEditor.Wpfを含むソリューション全体。
  - 全シェーダーが`fxc /Ges`(実行時と同じstrictness)でコンパイル成功。
  - **登場カットシーン中、敵の脚が見えて接地している**。以前は胴体が地面へめり込んだ塊に見えていたので、
    `GetAnimatedLocalMaxZ()`による接地補正が意図どおり効いている。
  - 戦闘状態でも敵・プレイヤーとも接地し、影が足元から連続している。
- **未確認のまま残っている項目**:
  - カットシーン→戦闘の0.36秒のカメラ遷移と回転クロスフェードの滑らかさ。
    120ms間隔で連続撮影したが、その頃には敵が密着しており構図から判断できなかった。
  - 壁が影を受けること。キャプチャした構図に闘技場の壁が入らなかった。
    壁際まで移動させる操作を自動化できていない。
- **気づいた点**: 敵が密着するとカメラの視界を塞ぐ(既知の未対応項目)。至近距離でプレイヤーが
  ドラゴンの体でほぼ隠れるため、カメラの遮蔽対応は体感への影響が大きそう。

### 2026-09-10 敵によるカメラ遮蔽への対応(Claude)

- **症状**: 大型の敵に密着すると、プレイヤーがドラゴンの体でほぼ隠れる。自分の位置も予兆も読めなくなる。
  実機キャプチャで確認した既知の未対応項目。
- **原因**: カメラ衝突(`UpdateArenaCameraCollision`)が闘技場の壁だけを対象にしており、敵を考慮していなかった。
- **修正**: [scene/GameScene.cpp](../scene/GameScene.cpp)に`CalcEnemyOccludedCameraDistance()`を追加。
  注視点からカメラ側へ伸ばした線分と敵を近似した球との交差を解き、遮蔽していれば手前で止める。
  壁の判定結果と`min`を取って`SetCollisionDistance()`へ渡すため、最終的な距離を決める場所は1箇所のまま。
  - **実際のカメラ位置と注視点を使う**([system/ThirdPersonCamera.h](../system/ThirdPersonCamera.h)に`GetPosition()`/`GetLookat()`を追加)。
    ロックオン時は注視点がプレイヤーと敵の中間寄りになるため、理想的なブームを再計算すると実際の構図とずれる。
  - 敵の半径は境界球そのものではなく**胴体の太さに近い値**(ローカルAABBのX幅の40%)にした。
    尻尾や翼まで含む境界球だと、少し離れただけでカメラが寄ってしまう。
  - 注視点が既に敵の内側にある場合と、敵がカメラより後方にある場合は引き寄せても改善しないため通常距離を返す。
- **採用理由**: 既存の`SetCollisionDistance`の仕組みへ相乗りするので、カメラ側に新しい状態を増やさない。
  半径をモデルの実頂点由来の境界から出しているため、敵モデルを差し替えても追従する。
- **限界**: 球による近似なので、細長い首や尻尾だけが視界を横切る場合は反応しない。
  対象は`m_enemies.front()`のみ。レイキャストではないため遮蔽物一般には対応していない。
- **確認**: Debug|x64ビルド成功(エラー0)。密着状態を実機キャプチャし、**プレイヤーが画面内に見えている**ことを目視確認済み。

### 2026-09-10 命中時の手応え(カメラシェイク・ヒットフラッシュ)(Claude)

- **狙い**: ヒットストップ以外の当たり反応が無く、命中も被弾も画面から分かりにくかった。
  モンハン系の手応えはVFX・ヒットストップ・カメラシェイクの比重が大きいので、その最初の一歩。
- **カメラシェイク** ([system/ThirdPersonCamera.h/.cpp](../system/ThirdPersonCamera.cpp)):
  - `TriggerShake(strength, duration)`を追加。時間に対して二次で減衰させ、軸ごとに周波数をずらして
    単純な往復ではなく不規則な揺れに見せる。注視点も0.35倍だけ揺らす(位置だけだと平行移動に見える)。
  - **揺れを含まない基準位置(`m_basePosition`/`m_baseLookat`)を別に持つ**のが要点。
    Codexが入れた指数補間は前フレームのカメラ位置を補間元にするため、揺れた位置をそのまま渡すと
    揺れが減衰せず残り続ける。補間は基準位置で行い、揺れは最後に足す。
  - 連続ヒットでは強い方だけを採用し、揺れが累積して画面が破綻しないようにした。
  - カットシーン遷移中も基準位置を更新する。しないと遷移終了時に古い基準位置から補間し直して跳ねる。
  - `Reset()`で揺れと補間状態を初期化(再戦の1フレーム目に前回の揺れが乗るのを防ぐ)。
- **敵のヒットフラッシュ**:
  - メッシュのマテリアルは`CStaticMeshRenderer::Draw()`が**サブセットごとに設定する**ため、
    描画前にマテリアルを差し替える方法では上書きされてしまう。
    そこで専用の定数バッファ(**b8** `TintBuffer`)を追加し、`Renderer::SetCharacterTint()`で設定する。
  - [shader/vertexLightingSkinPS.hlsl](../shader/vertexLightingSkinPS.hlsl)で、**影を掛けた後に**加算する。
    影の中でも光って見えるようにして、暗部で命中が分からなくなるのを防ぐ。
  - 描画後は必ず0へ戻す(他の描画へ影響しないため)。
- **被弾時の画面演出**: 画面の四辺だけを赤くする(`AddRectFilledMultiColor`で中央へ向けて透明化)。
  **中央を覆わない**ので、被弾中でも敵の予兆は読める。
- **強弱**: 強攻撃は1.5倍、敵の硬直(隙)へ入れた一撃は1.4倍にして、「読みが当たった」ことを体で分かるようにした。
  被弾は与ダメージより強く揺らす。
- **確認**: シェーダー全本`fxc /Ges`成功、Debug|x64ビルドエラー0。実機キャプチャを連続取得し、
  画面の輝度と画面端の赤みを数値で走査して該当フレームを特定 → **敵の発光と被弾時の赤い縁を目視確認済み**。

### 2026-09-10 剣の軌跡(VFX)と、モデル差し替えへの対応(Claude)

- **狙い**: 剣の軌跡を出す。同時に「別の剣やプレイヤーモデルへ差し替える可能性がある」という
  要件があるため、特定のモデルに依存しない作りにする。
- **新規 [system/WeaponTrail.h](../system/WeaponTrail.h)**(ヘッダのみ。vcxprojの変更が不要):
  - 受け取るのは**刃の根元と先端のワールド座標だけ**。この2点は`CAnimationMesh::GetSwordWorldSweep()`が
    **武器ボーンから**求めているため、剣モデルやプレイヤーモデルを差し替えても、
    `CharacterModelProfile`の`weaponBone`が解決できていればコード変更なしで動く。
  - 敵やほかのキャラクターの武器にも、インスタンスを1つ持たせるだけで使い回せる。
  - 頂点・インデックスは初期化時に最大数ぶん確保し、毎フレームは`Modify()`で中身だけ書き換える。
  - 新しいほど濃く、刃の根元へ向けて薄くしてリボンの内側を自然に消す。古さは二乗で減衰させ消え際を速くする。
  - 描画時は薄板なのでカリングを切り、半透明なので深度書き込みも切る。**描画後に必ず元へ戻す**。
  - ほとんど動いていないフレームは点を追加しない(同じ位置に面が重なって濃さがまだらになるため)。
- **発生条件**: 攻撃判定が出ている間だけ軌跡を残す。「刃が危険な時間」がそのまま光って見えるので、
  予兆と判定を読ませたいこのゲームの方針とも噛み合う。
- **調整で判明したこと**:
  - 最初は淡い色にしていたため軌跡が出ているか判別できなかった。**一時的に不透明の赤で強制描画**して
    描画経路が正常なことを確認してから、色と発生条件を戻した。推測で色を触り続けるより速い。
  - **ヒットフラッシュが強すぎた**。加算量0.85では敵全体が金色に塗り潰され、姿勢も予兆も読めなくなる。
    実機で確認しながら0.85→0.30→**0.16**まで落とした。暗いモデルへの加算は見た目への影響が大きい。
- **確認**: ビルドエラー0。実機キャプチャを走査して該当フレームを特定し、軌跡・フラッシュとも目視確認済み。
- **限界**: 軌跡は`unlitTexturePS`を共有しているため影の減光を受ける(影の中では少し暗くなる)。
  テクスチャを貼っていないので、単色のリボンとしての見た目にとどまる。

### 2026-09-10 法線のスキニング(Claude)

- **症状**: ボーンをどれだけ動かしても陰影が追従しない。体を捻っても明暗が変わらず、
  鎧が平坦な影絵のように見えていた。スキニング技術解説にも限界として記載していた項目。
- **原因**: 法線を**ワールド行列だけ**で変換しており、スキニング(ボーン変形)を通していなかった。
  位置だけがボーンで動き、法線は元の向きのまま固定されていた。
- **修正** ([shader/skinTransform.hlsli](../shader/skinTransform.hlsli)):
  - `SkinVertexHybrid()`を追加し、**位置と法線を同じスキニングから求める**ようにした。
    DQSは剛体変換なので、混合した回転クォータニオンで法線を回すだけでよい(`RotateByQuaternion`)。
    LBS側は行列の回転部分(3x3)だけを使う。法線は平行移動の影響を受けないため。
  - 混合後は長さが崩れるので正規化する。ゼロ長になった場合は元の法線へ戻す。
  - 既存の`SkinPositionHybrid()`は新関数を呼ぶだけの薄い関数として残した。
    影生成パスは法線を使わないので、未使用の計算はコンパイラが除去する。
- **採用理由**: 位置と法線を同じ関数・同じ重みで計算するため、変形と陰影が必ず一致する。
  法線用に別のスキニングを書くと、DQS比率などを変えたときに片方だけ更新する不整合が起きる。
- **確認**: 全シェーダー`fxc /Ges`成功、ビルドエラー0。実機キャプチャを拡大し、
  **肩・胸・脚の装甲に面ごとの明暗が出ている**ことを目視確認済み。

### 2026-09-10 ヒットエフェクト(火花)(Claude)

- **狙い**: モンスターハンターの参考画像に合わせ、命中点から火花を飛ばす。
  (当初はダメージ数値のポップアップも併せて実装したが、後述の通り削除した。)
- **新規 [system/HitEffect.h](../system/HitEffect.h)**(ヘッダのみ):
  - 受け取るのは**命中したワールド座標と弾く向きだけ**。プレイヤーの剣・敵の攻撃・
    差し替えた武器のいずれにも同じものを使える(`WeaponTrail`と同じ方針)。
  - 火花は常にカメラへ正対する板として描き、進行方向へ引き伸ばして線状に見せる。
    頭側を不透明・尾側を透明にすることで、板1枚で尾を引いた火花に見える。
  - 重力で落として飛び散りが下へ流れるようにし、消え際は二乗で速く消す。
  - 乱数生成器を持ち込まず、通し番号から角度と速さを散らしている(生成のたびに位相をずらす)。
- **ダメージ数値**: 一度実装したが、ユーザー方針により**削除した**(この作品では数値を出さない)。
- **命中点**: 剣先のワールド座標を使う。武器ボーン由来なので武器・モデルの差し替えに耐える。
  弾く向きは前フレームの剣先との差(刃の進行方向)から求める。
- **確認**: ビルドエラー0。実機キャプチャをオレンジ画素で走査して該当フレームを特定し、
  **火花の放射を目視確認済み**(この時点ではダメージ数値も表示していた)。

### 2026-09-10 ダメージ数値の削除(Claude)

- **狙い**: ユーザー方針「ダメージは出さなくていい」に合わせ、数値表示をやめる。
- **変更**: [scene/GameScene.cpp](../scene/GameScene.cpp)から生成箇所と
  `UpdateDamagePopups()` / `DrawDamagePopups()`の定義・呼び出しを削除。
  [scene/GameScene.h](../scene/GameScene.h)から`DamagePopup`構造体とメンバーを削除。
- **採用理由**: 数値が出ると「敵の挙動を見る」より「数値を見る」ゲームになる。
  この作品の核は敵のしぐさを読むことなので、手応えは火花・カメラシェイク・
  被弾発光といった**画面の中の反応**だけで伝える。
- **限界**: 与ダメージ量がプレイヤーに伝わらないため、
  強攻撃と弱攻撃の差はモーションの重さと敵の怯み方で表現する必要がある。

### 2026-09-10 火花の作り直し(「ちゃっちい」の解消)(Claude)

初版の火花は単色の板が並んでいるだけで安っぽかった。テクスチャを追加せずに質を上げるため、
実機で見ながら次の順で直した。**どれも一度失敗してから直している**ので、理由ごと残す。

1. **加算合成へ変更**(半透明合成→`BS_ADDITIVE`)。
   重なった火花ほど明るくなり、光って見える。半透明合成だと重ねても明るくならず、
   色紙を貼ったような見た目になる。
2. **2層構造**にした。「白熱した細い芯」と「その外側の広がった輝き」を重ねる。
   板1枚だとのっぺりして立体感が出ない。
3. **色を寿命で変える**(白熱→橙→赤)。温度が下がっていくように見せる。
4. **閃光を四角形1枚→放射する光条10本へ**。
   四角い板で光らせると、テクスチャが無いため**輪郭がそのまま箱として見えて**非常に安っぽい。
   中心を明るく先端を透明にした光条を放射状に並べると、板の輪郭が見えなくなる。
5. **大きさを実寸に合わせた**。当初は火花の長さが17〜31単位あり、キャラクター(約29単位)と
   同じ大きさだった。板が飛んでいるようにしか見えない。1.6〜6単位まで縮小。
6. **先端を点、尾を広がりにした彗星型へ**。当初は先端が幅広で、
   **平らな辺が見えるため木片のように見えた**。先端を点に近づけると同じ四角形1枚でも線に見える。

- **確認**: ビルドエラー0。実機キャプチャを走査して該当フレームを特定し、拡大して目視確認済み。
  細い光の線が放射状に飛び、根元に閃光が出ている状態になった。
- **調査の要点**: 被弾時の画面端の赤いビネットを「火花」と誤検出したため、
  走査範囲から画面端を除外する必要があった。色で探すときは他の演出との混同に注意する。

### 2026-09-10 他のPCでビルド・実行できるようにする(Claude)

- **狙い**: このリポジトリをクローンした別のPC、およびVisual Studioが入っていないPCで、
  手作業のセットアップ無しにビルド・実行できる状態にする。
- **まず実測した**: GitHubから実際にクローンし直して`Debug|x64`と`Release|x64`をビルドし、
  起動してモデルが表示されるところまで確認した。**この経路は元から通っていた**。
  - 心配していた`third_party/DirectXTK/lib`(約110MB)が未追跡である件は問題ではなかった。
    このプロジェクトはDirectXTKの`SimpleMath`の**ヘッダしか使っておらず**、
    ヘッダで解決できない`Matrix::Identity`等は`system/SimpleMathIdentity.cpp`で
    自前定義しているため、`DirectXTK.lib`はリンクしていない。
  - 実行時に要るAssimpのDLLはリポジトリのルートに追跡済みで置いてあった。
- **本当の問題はこちらだった**: `dumpbin /dependents`で依存を調べたところ、
  同梱していた`assimp-vc143-mtd.dll`が`ucrtbased.dll` / `MSVCP140D.dll`という
  **デバッグ版CRT**へ依存していた。これはVisual Studioが入っているPCにしか存在せず、
  再頒布も許可されていない。つまり**Visual Studioの無いPCでは起動できない**状態だった。
  さらにRelease構成もこのデバッグ版Assimpをリンクしており、
  実行ファイル(リリースCRT)とDLL(デバッグCRT)でCRTが食い違っていた。
- **変更**:
  - [system/AssimpPerse.cpp](../system/AssimpPerse.cpp): リンクするAssimpを`_DEBUG`で切り替え。
    Debugは`assimp-vc143-mtd.lib`、Releaseはリリース版の`assimp-vc142-mt.lib`。
  - リリース版のlibとDLLを追加(`third_party/assimp/lib/`とリポジトリのルート)。
  - 新規 [MakeDistribution.bat](../MakeDistribution.bat): Release|x64をビルドし、
    実行ファイル・リリース版AssimpのDLL・`shader`・`assets`を`dist`へまとめる。
  - [README_提出用.md](../README_提出用.md) と [third_party/README_ja.md](../third_party/README_ja.md)
    を実態に合わせて書き直した(従来は同梱していないファイルを「同梱している」と書いていた)。
- **採用理由**: 配布物からデバッグ版CRTへの依存を断つのが目的なので、
  リリース版Assimpへ差し替えるのが最短で確実。ヘッダのバージョン差を確認したところ、
  ABIに影響する構造体の定義差は無く、実際に違うのは`aiMetadata`の列挙値追加だけで、
  このゲームは`aiMetadata`を使っていない。
- **不採用案**:
  - デバッグ版CRTのDLLを配布物に同梱する案 — 再頒布が許可されていないため不可。
  - Debug構成のまま配る案 — 同じ理由で不可。
  - `third_party`にDirectXTKのlib(110MB)を追加する案 — リンクしていないので無意味。
- **限界**: `dist`は約695MBある。`assets`を丸ごとコピーしているためで、
  実際に読み込むモデルだけに絞れば大幅に減らせる(未対応)。
  リリース版AssimpはvC142(VS2019)ビルドを流用している。
- **確認**:
  - クローン直後のビルド: Debug|x64・Release|x64ともに終了コード0、エラー0。
  - `dumpbin /dependents x64\Release\DX11_3D_Game.exe` → デバッグ版CRT(`*D.dll`)への依存が消え、
    `MSVCP140.dll` / `VCRUNTIME140.dll`だけになった。
  - Release構成で起動し、**ドラゴンと騎士のモデル・影・剣の軌跡・命中の火花が
    描画されている画面をキャプチャで確認**(=リリース版Assimpでモデルが読めている)。
  - `MakeDistribution.bat`を実行し、生成された`dist`の中の実行ファイルを
    `dist`を作業フォルダにして起動 → 正常動作。

### 2026-09-10 敵の予兆を「HUDの文字」から「体の構え」へ移す(Claude)

- **背景(なぜこれを選んだか)**: 外部の方から
  「ゲームを一番アピールしたいなら、ゲームを作るために一番効率的なことは何かを考えた方がいい」
  という助言をもらった。この観点で残作業を見直したところ、次の状態になっていた。
  - 敵の攻撃は3種類あるが、モーションは`dragon_attack.dae`の**1本しか無く**、
    種類ごとの違いは**再生速度だけ**だった。つまり見た目はほぼ同じ。
  - そのためプレイヤーが「何が来るか」を知る手段は、**画面隅のHUDの文字**しか無かった。
  - 結果として、この作品が核だと言っている「敵のしぐさを見て決める」が成立しておらず、
    実際には「文字を読む」ゲームになっていた。
  → 新しいアセットも新しいツールも要らず、作品の核が最も動くのはここだと判断した。
- **新規 [system/EnemyAttackPose.h](../system/EnemyAttackPose.h)**(ヘッダのみ):
  攻撃の種類と段階(予兆/判定中/隙)と経過秒から、体の傾き・ひねり・高さのずらし量を返す。
  - **叩き付け**: 上体を後ろへ反らして伸び上がり、前へ叩き落とす。
  - **噛みつき**: 低く沈んで鼻先を落とし、そのまま突っ込む。
  - **薙ぎ払い**: 体を大きく横へひねって溜め、反対側へ振り抜く。
  - 隙(Recovery)では振り抜いた体勢のままゆっくり戻す。これが反撃の合図になる。
- **適用先は描画用SRTだけ**([gameobject/enemy.cpp](../gameobject/enemy.cpp)の`getRenderSRT()`)。
  物理SRT・壁との衝突・敵AIの判断には影響させない。
  演出とゲーム内の位置を混ぜると、壁抜けや接地ずれという別の不具合を呼ぶため。
- **当たり判定を見た目に合わせた**([system/OneVsOneCombat.cpp](../system/OneVsOneCombat.cpp)):
  敵の攻撃判定はこれまで**距離だけ**で、真後ろにいても噛みつきが当たっていた。
  正面からの角度で絞るようにし、噛みつき約35度・叩き付け約55度・薙ぎ払い約110度とした
  (`Combat::EnemyHitHalfAngleOf()`)。これで「横へ回り込む」という選択が意味を持つ。
  敵の向きは`OneVsOneCombat::SetEnemyFacingYaw()`で渡す。
  **必ず物理SRTの向きを渡すこと**。描画用SRTには演出のひねりが入っている。
- **調整用の切り替えを追加**: `dev_settings.ini`の`force_enemy_attack=slam|bite|sweep`、
  およびデバッグ表示の「Force enemy attack」。通常の攻撃選択は距離と直前の攻撃で変わるため、
  狙った攻撃が出るまで待つことになり、構えを見比べられなかった。
- **採用理由**: 新規モーションを追加できない制約の下で、遠目のシルエットを変える最短の手段が
  体全体の向き・傾き・高さを手続き的に足すことだった。ボーン単位で足す案も考えたが、
  ドラゴンのボーン名に依存するため、モデルを差し替えると動かなくなる。
  体全体への加算ならモデルに依存しない。
- **不採用案**:
  - 攻撃ごとに別クリップを用意する案 — 新規モーションアセットを追加できないため不可。
  - 再生速度だけで差を出す案(従来) — 実機で見て区別できなかった。
  - HUDの文字を充実させる案 — 「文字を読むゲーム」を強化するだけで、核の解決にならない。
- **限界**: クリップは1本のままなので、体の向きは変わっても手足の運びは同じである。
  溜めを大きくしすぎると足が付いていかないため、実機で見て区別できる最小限に留めている。
  攻撃判定は今も1点(距離+角度)であり、部位ごとの判定ではない。
- **確認**: ビルドエラー0。`force_enemy_attack`で3種類を個別に起動し、実機キャプチャで比較。
  - 叩き付け: 立ち上がって腹を見せる姿勢を確認。
  - 薙ぎ払い: 体を横へ向けて側面を見せる姿勢を確認。
  - 噛みつき: **初期値(前傾0.20 / 沈み込み-3.4)では叩き付けと見分けが付かなかった**。
    前傾0.34 / 沈み込み-7.5へ強めて、低く鼻先を突き出す姿勢になったことを確認した。
  - 影もポーズに追従しており、シルエットの違いが影にも出ている。

### 2026-09-10 攻撃中に敵が地面へ埋まる / カットシーンの影が合わない(Claude)

ユーザー報告:「敵が攻撃の時うまるときがある」「最初のカットシーンの影とエネミーがあってない」。
どちらも、直前に入れた「攻撃ごとの構え」の影響と、影の範囲の前提が原因だった。

#### 1. 攻撃中に敵が地面へ埋まる

- **原因**: 敵の接地は毎更新で**スキニング後の頂点のローカルZ最大値**から求めていた
  (`GetAnimatedLocalMaxZ()`)。これは「モデルをX軸へ+90度回したとき、
  ローカル+Zがワールドの下を向く」という前提に立っている。
  ところが構えの演出で**さらにピッチを足した**ため、この前提が崩れた。
  傾ければ最下点は別の頂点へ移るのに、接地は傾ける前の最下点で計算されていた。
  加えて、沈み込みを高さの直接加算(噛みつきで-7.5)で表していたため、
  接地補正を無視してそのまま地面へめり込んでいた。
- **修正**:
  - [system/CAnimationMesh.cpp](../system/CAnimationMesh.cpp)に
    `GetAnimatedLowestLocalHeight(extraPitchRadians)`を追加。
    追加ピッチを含めた実際のワールドY最小値を、同じ頂点走査で求める。
    行ベクトル規約でX軸へ(90度+p)回すと高さは `-y*sin(p) - z*cos(p)` になる。
    p=0のとき`-maxZ`と一致するので、従来の式はこの一般形の特殊ケースになった。
    `GetAnimatedLocalMaxZ()`はこの関数の符号反転として残してある。
  - **姿勢オフセットから高さ(lift)を削除した**。接地を毎更新でやり直している以上、
    高さを直接足すのは必ず食い違う。傾けさえすれば接地側が最下点を追うので、
    沈み込みも伸び上がりも結果として付いてくる。
- **採用理由**: 「見た目の高さを足す」のをやめ、**接地の計算そのものを演出に対応させた**。
  高さを足す側でクランプする案も考えたが、傾きによる沈みは残るため対症療法にしかならない。
- **確認**: `force_enemy_attack`で叩き付け・噛みつきを個別に起動し、
  予兆・振り下ろしの各段階で**足が地面に接したまま**であることをキャプチャで確認。
  接地を厳密にした副作用で浮き上がらないことも合わせて確認した。

#### 2. カットシーンの影とドラゴンが合わない

- **原因**: 影の範囲(正射影)は**プレイヤーを中心**に一辺260(±130)で作っていた。
  カットシーン中はカメラがドラゴンを映しており、プレイヤーは画面外にいる。
  ドラゴンの着地点はz=-120で、体長が約43あるため**範囲の外側へはみ出し**、
  はみ出した部分の影が落ちていなかった。落下中はさらに高い位置にいるため、
  平行光源の向き(0.5,-1,0.8)の分だけ影の落ちる位置が本体から大きく離れ、
  こちらも範囲外だった。
- **修正**: [scene/GameScene.cpp](../scene/GameScene.cpp)の`BuildLightViewProjection()`で、
  `m_enemyIntroActive`の間はドラゴンを中心にし、範囲を460へ広げる。
  戦闘中は従来どおりプレイヤー中心・260のまま(解像度を戦闘へ集中させるため)。
- **不採用案**: 常に範囲を広げる案 — 影の解像度が落ちて戦闘中の輪郭が粗くなる。
  カットシーン中だけ影を消す案 — 着地の重さが出なくなる。
- **確認**: カットシーンをキャプチャし、着地したドラゴンの足元から影が繋がり、
  シルエットが本体と一致していることを確認した。

### 2026-09-10 デバッグ表示を日本語化し、1つのウィンドウへまとめる(Claude)

- **狙い**: ユーザー要望「ImGUIのデバッグを日本語にしてほしいのと束ねれるようにしてほしい」。
  デバッグ表示は7つの独立したウィンドウに分かれており、画面中に散らばって
  ゲーム画面がほとんど見えなくなっていた。表記も英語のままだった。
- **まとめ方**: [scene/GameScene.cpp](../scene/GameScene.cpp)に`DrawDebugWindow()`を追加し、
  1つの「デバッグ」ウィンドウの中にタブ(戦闘/当たり判定/敵/プレイヤー/カメラ/音量/壁)を並べた。
  各パネルは`ImGui::Begin`→`ImGui::BeginTabItem`へ置き換えている。
  - 当たり判定パネルは`DebugCombat()`から`DebugCollision()`へ分離した。
  - 3D空間へ重ねる当たり判定のラベルは`DrawCollisionWorldLabels()`へ切り出し、
    タブを開いていなくても描かれるようにした。見たいのは画面の中のキャラクターであり、
    タブの中身ではないため。
- **ドッキングも有効にした**([system/DebugUI.cpp](../system/DebugUI.cpp))。
  デバッグウィンドウのタイトルバーを別のウィンドウへ重ねると1つにまとまり、
  タブで切り替えられる。モーションエディタや剣の取り付けデバッグなど、
  他の場所から登録されるウィンドウもこれで束ねられる。
  - **画面全体を覆うドックスペースは作っていない**。作るとゲーム画面の上に透明な受け皿が乗り、
    左クリック攻撃などの入力を吸ってしまうため。以前、ImGuiのキーボードナビを有効にしたら
    回避とダッシュが効かなくなった前例がある。
- **日本語化**: 各ラベルに加えて、状態名も日本語にした。
  `player::getMotionStateName()` / `enemy::getMotionStateName()` /
  `OneVsOneCombat::GetStateName()`。
  敵の攻撃名は、HUDが英字表記で統一されているため`EnemyAttackDebugName()`を別に用意し、
  デバッグ表示だけ日本語にしている。
- **フォント**: 同梱の`assets/font/NotoSansJP-Light.otf`を先に試し、
  無ければメイリオ→游ゴシック→MSゴシック→ImGui既定フォントの順に落とす。
  従来は`C:\Windows\Fonts\meiryo.ttc`直指定だった。メイリオは日本語版Windowsにしか無いため、
  他のPCで動かす方針(2026-09-10)と噛み合わない。存在しないファイルを渡すとImGuiが
  アサートで止まるので、開けるかどうかを先に確かめている。
- **踏んだ罠**: マルチビューポートが有効なため、**ImGuiのウィンドウ位置は画面全体の座標**である。
  `SetNextWindowPos(ImVec2(10,10))`のように小さな値を渡すと、
  ゲームウィンドウの外側に別のOSウィンドウとして開く(実際にそうなっていた)。
  `GetMainViewport()->WorkPos`を基準にすること。
  また`ImGuiCond_FirstUseEver`は`imgui.ini`に保存済みの位置を上書きしないため、
  一度外へ出た状態が保存されていると直らない。該当エントリを消して確認した。
- **確認**: Debug/Releaseともビルドエラー0。実機キャプチャで、
  1つの「デバッグ」ウィンドウにタブが並び、すべて日本語で表示され、
  体力ゲージと重ならない位置に出ていることを確認した。

## 進行中 / 次の予定

- 火花のさらなる質感向上が必要ならテクスチャ(点が滲んだ円)を用意する。
- 未確認項目の目視確認: カットシーン→戦闘の遷移の滑らかさ、壁の影(ユーザー判断で優先度低)。
- 法線のスキニング(現在ボーン変形が陰影に反映されず、体を捻っても陰影が追従しない)。
- Issue I: 見切りボーナスはユーザー方針により見送り。
- 引き継ぎ用資料: [docs/Codex引き継ぎメモ.md](./Codex引き継ぎメモ.md)

### 2026-09-09 戦闘カメラの第一段階 — Elden Ring / Monster Hunterを参考にした構図と遷移

- **狙い**: プレイヤーの背後を基準にした操作感を残しながら、ロックオン中に敵・自分・回避方向を同時に読みやすくする。登場カットシーンから戦闘へ移る瞬間のカメラの跳ねも抑える。
- **変更**:
  - [system/ThirdPersonCamera.h/.cpp](../system/ThirdPersonCamera.cpp): 通常追従とロックオン追従を指数補間し、敵の移動でカメラが毎フレーム即座に首を振らないようにした。
  - ロックオン構図では、プレイヤーと敵の中間より少しプレイヤー側の高い点を注視し、カメラをプレイヤーの後方へ置いたまま敵方向に対して6単位だけ肩越しへずらす。距離は敵との間隔に応じて76〜112単位へ収める。
  - 攻撃中の距離縮小(`m_lookDistance`)をロックオン構図にも反映した。通常時とロックオン時で攻撃ズームの有無が分かれる不整合を解消した。
  - `BeginBattleTransition()`を追加し、カットシーンの現在位置・注視点から戦闘用カメラへ0.36秒のsmoothstep補間で切り替える。従来の`Reset()`による瞬間移動は使わない。
  - 中央ボタンを押している間は、マウス移動でカメラを周回できるようにした。右クリックは強攻撃に割り当て済みのため、操作競合を避けた。`GetYaw()`は周回分も含め、プレイヤー移動とカメラの向きを一致させる。
  - 再戦時は前回の攻撃ズーム距離をカットシーン後へ持ち越さず、基準距離へ戻す。
- **採用理由**: Elden Ringのようにプレイヤー後方を基準にして移動方向を安定させ、Monster Hunterのように大型敵との相対関係を中間注視点へ収める方針にした。追従を指数補間にするとフレームレートに依存しにくく、固定係数の線形補間より60fps以外でも応答が安定する。
- **不採用案・欠点**:
  - カットシーン終了時に`Reset()`で即時切り替える案は、咆哮直後に画面が跳ねるため不採用。
  - ロックオン時にカメラをプレイヤーと敵の中間へ完全に置く案は、敵との距離が大きいとプレイヤーがニアクリップ付近へ隠れやすいため不採用。プレイヤー後方を維持し、注視点だけ中間へ寄せた。
  - 右ドラッグを周回操作にする案は、右クリック強攻撃と同じ入力になるため不採用。中央ドラッグは既存戦闘入力と衝突しない一方、一般的なマウス操作としては慣れが必要である。
  - 敵の遮蔽判定や画面内の敵スクリーン座標による高度な自動フレーミングはまだ実装していない。現在はロックオンの相対距離と闘技場外周のカメラ衝突補正を組み合わせている。
- **限界**: ロックオン対象は現在1体固定。実際の壁面とのレイキャストではなく闘技場を円として近似しているため、壁の角や遮蔽物に対する完全なカメラ押し戻しではない。肩越しオフセットは固定6単位なので、敵との距離や画面アスペクト比によっては最適値が変わる。手動周回は中央ボタン操作時のみで、ロックオン中は相対構図を優先して無効にしている。
- **検証**: `MSBuild.exe DX11_3D_Game.sln /t:DX11_3D_Game /p:Configuration=Debug /p:Platform=x64`成功(終了コード0、エラー0)。修正版を起動し、プロセス応答あり・異常終了なしを確認。UIキャプチャ環境ではゲーム本体の描画ウィンドウを取得できず、0.36秒の遷移と中央ドラッグの実画面上の最終見た目は継続確認とする。

### 2026-09-10 攻撃開始・終了のアニメーション補間と歩行中攻撃のブレンドツリー

- **背景**: キーフレーム間の回転補間と、下半身の移動・上半身の攻撃を重ねる処理は既に存在した。しかし、FBX攻撃へ切り替える直前の姿勢を正しく保存していなかったため攻撃開始時に姿勢が跳び、攻撃終了時も通常の歩行・待機へ即時に戻っていた。また、攻撃中は移動入力を常にロックしていたため、実際のゲーム操作では歩きながら攻撃できなかった。
- **変更**:
  - `CAnimationMesh`に現在のローカル姿勢を取得する`CaptureCurrentLocalPose()`を追加し、攻撃開始時に次の更新フレームで実際のメッシュ姿勢を保存するようにした。
  - 攻撃開始時は保存した姿勢から攻撃上半身へ0.10秒のsmoothstep補間を行う。下半身は歩行・待機モーションを維持し、足元の接地を崩しにくくした。
  - 攻撃終了時は、その時点の攻撃姿勢から歩行・待機姿勢へ0.10秒で補間するようにした。
  - `CharacterAnimationState`へ`deltaSeconds`を渡し、アニメーション時間とフレーム進行を固定1/60秒ではなく実時間に追従させた。
  - WASD入力を押している攻撃中だけプレイヤー移動のロックを解除し、下半身の歩行・待機と上半身の攻撃を同時に再生できるようにした。入力を離した攻撃中は従来どおり移動を止める。
- **採用理由**: 既存のローカルボーン姿勢とレイヤー構造を活かせるため、モデル資産や攻撃モーションの作り直しなしで切り替え時の違和感を減らせる。上半身だけを補間することで、敵が埋まる・足が浮く問題につながりやすい腰・ルート・下半身の攻撃姿勢をゲーム移動から分離できる。
- **不採用案・欠点**:
  - 全身攻撃モーションへ一括で補間する案は、攻撃FBXのルート・腰・足までゲーム移動と競合して接地が崩れるため不採用。
  - 姿勢を保存せず即時に攻撃へ切り替える案は、既存の姿勢跳びを解消できないため不採用。
  - 既存の`CAnimationMeshBlender`をこの経路の中心にする案は、現在のプレイヤー更新が必要とする「選択ボーンだけ」「現在姿勢から」の制御を持たず、攻撃終了時のロコモーション復帰も扱いにくいため採用しなかった。
  - 攻撃中は常に移動可能にする案は、入力していないのに攻撃のルート移動でキャラクターが滑る可能性があるため、WASD入力時だけ許可した。
- **限界**: 攻撃の補間時間は現在0.10秒固定。足IKや足接地補正は未実装で、歩行から走行への遷移もまだ即時切り替えである。攻撃モーション固有の腰・ルート移動は意図的に下半身へ適用していない。
- **確認**: `Debug|x64`ビルドは終了コード0・エラー0。修正版ゲームのプロセス起動・応答を確認した。UIキャプチャ環境ではゲーム本体の連続描画を取得できなかったため、歩行中攻撃の足接地、攻撃開始・終了の見た目は実機操作で継続確認する。

### 2026-09-10 攻撃中の移動と回避移動の引き継ぎを禁止

- **症状**: 前回の歩行中攻撃対応により、W/A/S/Dを押しながら攻撃するとプレイヤーが移動できていた。また、回避中に攻撃入力を受け付けると、`player::update()`が回避移動を優先して早期returnする一方、戦闘システムと攻撃アニメーションは開始するため、回避の移動量を持ったまま攻撃できていた。
- **修正**:
  - 攻撃中はW/A/S/D入力の有無に関係なく`lockPlayerMovement`を有効にし、その場攻撃に戻した。上半身攻撃＋下半身ロコモーションの姿勢合成は残すが、ワールド移動とは分離する。
  - `playerDodgeActive`を攻撃入力判定より先に取得し、回避中の左クリック・右クリックを`OneVsOneCombat::Update()`へ渡さないようにした。回避中に攻撃アニメーションだけが開始する経路をなくした。
  - 回避開始と同フレームの攻撃入力も、攻撃開始を優先して回避移動が始まることがないよう、入力を同じゲートで処理する。
- **原因**: 問題はプレイヤーの速度を攻撃後に打ち消す処理ではなく、攻撃受付・回避処理・プレイヤー移動処理の順番にあった。回避状態を考慮せず攻撃だけ先に開始していたことと、攻撃中の移動ロックを入力保持時だけ解除していたことが原因。
- **採用理由**: 攻撃・回避はどちらも足元の移動責務を持つため、同時に成立させず、攻撃中は停止、回避中は攻撃無効という明確な排他にした。これにより、攻撃判定・アニメーション・ワールド移動の状態が一致する。
- **不採用案・欠点**:
  - 攻撃開始後に回避移動量だけを後から0にする案は、同フレームの位置更新やアニメーション開始が既に進んでおり、入力競合を根本的に防げないため不採用。
  - 攻撃中の移動を許可し続ける案は、今回の仕様に反し、歩行しながら攻撃する操作を残すため不採用。
  - 回避中の攻撃を受け付けてから攻撃アニメーションだけ止める案は、戦闘システムの攻撃状態と表示がずれるため不採用。
- **確認**: `Debug|x64`ビルドは終了コード0・エラー0。修正版ゲームを起動し、プロセス応答あり・異常終了なしを確認した。実機で「攻撃中にWASD」「回避中にクリック」を操作する最終目視確認は継続する。

### 2026-09-10 密着攻撃時にプレイヤーが巨大化して見える不具合の修正

- **症状**: 敵に密着して攻撃すると、スクリーンショットのようにプレイヤーの装備・腕が画面の大部分を占め、モデル自体が拡大したように見える。
- **原因**:
  - プレイヤーのワールド`SRT.scale`は初期化時の`CharacterModelProfile.modelScale`(10,10,10)から攻撃中に変更されていなかった。したがって、攻撃処理が直接プレイヤーのワールド倍率を書き換えた不具合ではない。
  - 直前に追加した攻撃クロスフェードが、ローカルボーン行列から回転と移動だけを取り出して再構成していた。行列に含まれるボーン固有スケールを捨てるため、攻撃開始時に姿勢によっては身体の一部の倍率が変わる経路になっていた。
  - ロックオン中の攻撃ズームも、密着時にフレーミング距離76へ攻撃倍率0.62を掛け、47.12単位まで寄る計算だった。これはワールド倍率を変えないが、プレイヤーの実寸に対してカメラが近すぎ、巨大化して見える二次要因になっていた。
- **修正**:
  - `CAnimationMesh`と`CCharacterAnimator`にSRT補間を追加し、行ベクトルの各行長からX/Y/Zスケールを復元して、スケール・回転・移動をまとめて補間するようにした。補間率0/1では元行列をそのまま返すため、遷移端でも倍率が変わらない。
  - ロックオン戦闘カメラに最低距離64単位を追加した。遠距離では従来の攻撃ズームを維持し、密着時だけ過度な寄りを防ぐ。
- **採用理由**: ワールドSRTの倍率を場当たり的に戻すのではなく、原因となったローカルボーン行列の情報欠落を直すことで、攻撃以外の姿勢遷移にも同じ安全性を適用できる。カメラ最低距離はモデル倍率を変更せず、構図だけを保護できる。
- **不採用案・欠点**:
  - 攻撃中にプレイヤーのワールド`scale`を小さくする案は、衝突寸法・剣の接続・影の大きさと表示が不一致になるため不採用。
  - 攻撃ズーム自体を無効にする案は、遠距離時の攻撃の迫力を失うため不採用。密着時の最低距離だけを設けた。
  - ボーン行列を要素ごとに線形補間する案は、回転の補間が不自然になり、非直交行列やスケール混在時の姿勢が崩れるため不採用。回転は正規化してクォータニオン`slerp`、スケール・移動は線形補間する。
- **限界**: 負のスケールやせん断を含む特殊なボーン行列は対象外。カメラ最低距離64は現在のプレイヤー寸法と45度FOVに合わせた固定値で、モデル差し替え時は再調整が必要。壁の衝突補正がさらにカメラを押し込むケースは別途確認する。
- **確認**: `Debug|x64`ビルドは終了コード0・エラー0。修正版ゲームを起動し、プロセス応答あり・異常終了なしを確認した。ゲーム本体の連続描画を取得できない環境のため、密着攻撃時の見た目は実機操作で最終確認する。

### 2026-09-13 Codex実装3件の実機確認(Claude)

- Codexの3件(攻撃開始・終了の補間 / 攻撃中の移動禁止 / 密着時の巨大化修正)は、
  いずれも「ゲーム本体の描画を取得できないため見た目は継続確認」となっていた。
- ロックオン中・密着状態で3段コンボを出し、60ミリ秒間隔でキャプチャして確認した。
  - 振り上げ→振り抜き→待機へ戻るまで姿勢の跳びは無く、体の比率も一定。巨大化は起きていない。
  - 攻撃中の移動禁止は、`lockPlayerMovement`が攻撃中は入力に関わらず真になり、
    回避中の攻撃入力を戦闘システムへ渡さない実装であることをコードで確認した
    (移動しないことの画像上の確認は、カメラが追従するため判別しにくく行っていない)。
- 確認後、それまでの変更と合わせてコミットした(`4e6a62f`)。

### 2026-09-13 敵の怯みとプレイヤーの吹き飛ばし(Claude)

- **狙い**: 予兆は体の構えで読めるようにしたが、その結果が「画面の揺れと発光」だけで、
  読みが当たったか外れたかが体で伝わらなかった。怯みを「読みが当たったご褒美」、
  吹き飛ばしを「読み違えた代償」として、キャラクターの体で返す。
- **敵の怯み**:
  - [system/CombatAttackTable.h](../system/CombatAttackTable.h): プレイヤーの攻撃に怯み値
    (`AttackData::postureDamage`。弱14/強34)を設定。しきい値60、隙へ入れると2倍、
    毎秒6ずつ抜ける、怯み時間0.9秒。
  - [gameobject/enemy.cpp](../gameobject/enemy.cpp): `MotionState::Flinch`を追加。
    `addPostureDamage()`で溜め、超えたら怯む。怯み中は向き直りもしない(背後へ回る機会)。
    明けたら様子見へ戻す(後退させると、反撃に踏み込んだプレイヤーから逃げてしまうため)。
  - [system/EnemyAttackPose.h](../system/EnemyAttackPose.h): `EnemyFlinchPose()`。
    0.07秒で一気にのけぞり、残り時間で戻す。打たれた側と反対へ顔を背ける。
    `getAttackPoseOffset()`経由で返すので、接地計算も傾きを織り込む(埋まらない)。
  - [system/OneVsOneCombat.cpp](../system/OneVsOneCombat.cpp): `CancelEnemyAttack()`。
    怯んだら戦闘側の攻撃も取り消す。取り消さないと、のけぞっている敵からダメージが飛んでくる。
- **プレイヤーの吹き飛ばし**:
  - [gameobject/player.cpp](../gameobject/player.cpp): `applyKnockback()`と`MotionState::Knockback`。
    回避と同じく`update()`の中で`m_srt.pos`へ足すので、GameSceneの壁補正を通る。
    速度は二乗で落ちる。飛ばしてきた敵の方を向かせ、上体を後ろへ0.30ラジアン反らせる。
  - 攻撃ごとの強さは`Combat::EnemyKnockbackOf()`。叩き付け約77/薙ぎ払い約51/噛みつき約11。
  - [system/CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp): `PlayImpactMotion()`。
    既存の`sword and shield impact.fbx`を上半身レイヤーで再生する(脚は待機のまま)。
    `dev_settings.ini`の`impact`キーでクリップを差し替えられる。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): 飛ばされている間は攻撃・回避の入力を受け付けず、
    無敵にする(飛ばされた先で次の攻撃に拾われ続けないように)。脚が歩行を刻まないよう、
    アニメーターへは「移動していない」として渡す。
  - デバッグ表示の戦闘タブに「敵の怯み値」バーを追加。
- **踏んだこと**: 初版の吹き飛ばし(叩き付け初速260=約48)は実機で「押し戻された」程度にしか見えず、
  初速420(約77)へ強めた。距離は 初速 x 時間 / 3 で決まる。
- **採用理由・不採用案**: [docs/技術解説.md](./技術解説.md)の該当節にまとめた
  (1発ごとに怯ませる案、位置を直接書き換える案、全身クリップを再生する案を不採用とした理由)。
- **限界**: ダウン(転倒→起き上がり)は無い。全身を動かすクリップは脚のIKが無いと接地が崩れる。
  被弾ののけぞりクリップ(0.6秒)が終わるまで移動が固まるため、噛みつきのように
  飛ばし時間が短い攻撃では、飛ばされ終わってから少し動けない時間がある。
- **気付いたこと(未対応)**: 敵の体力が100で、強攻撃40が3発当たると倒れる。
  怯みのしきい値60は強攻撃2発分なので、実際の戦闘で怯みを見られるのは1回程度しかない。
  怯みを読み合いの一部として機能させるなら、敵の体力の見直しが要る。
- **確認**: `Debug|x64`ビルドエラー0。実機キャプチャで、叩き付けを食らって敵の方を向き
  のけぞりながら後ろへ飛ばされること(足は接地したまま)、強攻撃2発で怯み値が超えた瞬間に
  敵が顔を背けてのけぞり0.9秒で戻ること、怯み値バーが溜まって抜けることを確認した。

### 2026-09-14 敵の体力を増やし、怯み耐性を入れる(Claude)

- **狙い**: ユーザー要望「敵の体力はエルデンやモンハンみたいにもっと多くしたい」。
  以前はプレイヤーと敵が同じ体力100を共有しており、強攻撃(40)3発で敵が倒れていた。
  予兆を読んで立ち回る時間がほとんど無く、怯みも1戦で1回程度しか起きなかった。
- **変更**:
  - [system/CombatAttackTable.h](../system/CombatAttackTable.h): `PLAYER_MAX_HP = 100` と
    `ENEMY_MAX_HP = 2000` に分けた。[system/OneVsOneCombat.h](../system/OneVsOneCombat.h)の
    共有定数`MAX_HP`を廃止し、`GetPlayerMaxHp()` / `GetEnemyMaxHp()`を追加。
    HUDとデバッグ表示で`/ 100.0f`と直書きしていた割合計算をこれに置き換えた
    (直書きのままだと敵の体力バーが最初から20倍で振り切れる)。
  - **怯み耐性**: 怯むたびにしきい値を1.25倍に上げ、375で止める(150→188→234→293→366→375)。
    [gameobject/enemy.cpp](../gameobject/enemy.cpp)の`getFlinchThreshold()`。
- **値の根拠**: 敵の攻撃1周期(予兆→攻撃→隙→様子見)は約4秒。隙へ毎回3段コンボを
  入れられるわけではないので、平均の与ダメージを秒間12程度と見積もり、
  予兆を読めるプレイヤーで約2分半〜3分の戦闘になる2000とした。
- **怯み耐性を入れた理由**: 体力2000のまま固定しきい値60だと、与える怯み値の総量から
  1戦で40回以上怯む計算になり、敵が何もできないまま殴られ続ける。
  これは怯みを溜める方式にした理由(一方的な戦いを避ける)そのものを壊す。
  モンスターハンターの怯み耐性と同じく、怯むたびに上げて1戦で8回前後に収めた。
  序盤は怯ませやすく、終盤ほど狙って溜めないと怯まない。
- **不採用案**: プレイヤーの与ダメージを下げる案 — 1発の手応えが弱くなるため、敵の体力側で調整した。
  固定しきい値を大きくする案 — 序盤の怯みが遠くなり、最初の「読みが当たった」体験が遅れる。
- **確認**: `Debug|x64`ビルドエラー0。強攻撃を連打し続ける操作で15秒間キャプチャし、
  デバッグ表示で怯み値が 150 → 怯み → 188 → 怯み → 234 と上がっていくことを確認した。
  以前は同じ操作で敵が先に倒れていたが、今回は回避しないプレイヤーが先に力尽きた(HUNT FAILED)。
- **限界・未対応(ユーザー判断待ち)**:
  - **回復手段が無い**。プレイヤーの体力100に対して敵の攻撃は10〜22なので、約6発で倒れる。
    2分半〜3分の戦闘で敵は約40回攻撃してくるため、およそ85%を避け続ける必要がある。
    エルデンリングの聖杯瓶やモンハンの回復薬に当たるものが無いと、初見の人には厳しい。
  - **敵の体力がゲーム画面に出ていない**(デバッグ表示のみ)。エルデンリングはボスの体力バーを出し、
    モンハンは出さない。長い戦闘でどちらにするかは設計の判断。

### 2026-09-14 敵の弱り具合を体の動きで見せる(体力ゲージの代わり)(Claude)

- **狙い**: ユーザー方針「体力ゲージは出したくない。減ってるとかは敵のしぐさで見せたい」。
  体力を2000に増やして戦闘が長くなったため、残り体力が分からないと
  「効いているのか」が伝わらない。モンスターハンターのように、体の変化で伝える。
- **弱り具合を3段階にした**([system/CombatAttackTable.h](../system/CombatAttackTable.h)):
  - 通常(体力50%より上)
  - 疲れ(50%以下): 頭が少し下がり、息で上下する。移動0.85倍、攻撃後の隙1.2倍。
  - 瀕死(20%以下): 頭が大きく下がり、歩くと一歩おきにつんのめる(足を引きずる)。
    止まっているときは肩で息をする。移動0.55倍、攻撃後の隙1.5倍。
  - 段階が悪くなった瞬間は、怯みと同じ動きでよろめく(怯み値や怯み耐性の回数には数えない)。
    画面を揺らし、瀕死に入った瞬間だけ咆哮させる。
- **実装**:
  - [system/EnemyAttackPose.h](../system/EnemyAttackPose.h): `EnemyCondition`と`EnemyConditionPose()`。
  - [gameobject/enemy.cpp](../gameobject/enemy.cpp): `setHealthRatio()`で弱り具合を更新。
    移動の速さ(`moveSpeedScale()`)と隙の長さ(`recoverySeconds()`)に反映。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): 毎更新で残り体力の割合を敵へ渡す。
    よろめいたら戦闘側の攻撃も取り消す。歩きのアニメーションも移動の速さに合わせて遅くする
    (合わせないと足が地面を滑って見える)。
- **設計上の判断**:
  - **予兆と攻撃判定の間は弱り具合の姿勢を足さない**。息で頭が上下すると構えの形が崩れ、
    「何が来るか」が読みにくくなる。予兆の読みやすさを、弱り具合の表現より優先した。
  - **高さとロール(横倒し)は使わない**。接地計算はピッチしか織り込んでいないので、
    高さを足すと埋まり、ロールを足すと片側の足が沈む。足を引きずる様子は
    一歩ごとの前のめり(ピッチ)と体のぶれ(ヨー)で表した。
  - **隙を伸ばすのは敵AIの側だけ**。戦闘判定側の攻撃は先に終わって待機へ戻るので、
    次の攻撃の受け付けと食い違わない。逆(戦闘側だけ伸ばす)にすると、
    敵が次の予兆に入っても戦闘側が受け付けず、ダメージの無い攻撃が出てしまう。
  - 攻撃の踏み込みの速さには倍率を掛けない。掛けると攻撃の届く距離と見た目が食い違う。
- **調整用**: `dev_settings.ini`の`enemy_hp`で試合開始時の敵の体力を指定できる。
  デバッグ表示の戦闘タブに「敵の弱り具合 (体力%)」とスライダーを追加。
- **ついでに直した不具合**: 「デバッグ」「動作状況」ウィンドウがゲーム画面の外に出ていた。
  マルチビューポートではウィンドウ位置が画面全体の座標で`imgui.ini`へ保存されるため、
  起動ごとにゲームウィンドウの位置が変わると、前回の場所に取り残される。
  `ImGuiWindowFlags_NoSavedSettings`で位置を保存しないようにし、起動のたびにゲーム画面基準で置き直す。
- **確認**: `Debug|x64`ビルドエラー0。
  - `enemy_hp=380`(19%)で起動し、デバッグ表示で「瀕死」を確認。歩くと一歩ごとに頭が沈んで
    つんのめり、足は接地したままであることをキャプチャで確認した。
  - `enemy_hp=1010`(50.5%)で起動して強攻撃を当て、体力48%になったフレームで
    弱り具合が「疲れ」に変わり、同時に敵AIが「怯み」(よろめき)に入ることを確認した。
- **限界**: 疲れの段階の見た目(頭の下がり0.05、息0.04)は控えめで、単独では気付きにくい。
  気付かせる役は段階が変わった瞬間のよろめきが担っている。
  弱り具合は体力だけで決まり、時間経過の疲労(モンハンのよだれ)や怒り状態は無い。

### 2026-09-15 回復手段は保留(ユーザー判断)

- 体力2000の長い戦闘に対して回復手段が無い点(約6発で倒れる)を相談したところ、
  ユーザー判断で「いったん保留」となった。回復アイテム・プレイヤー体力の変更は行っていない。

### 2026-09-15 F3のゲーム内モーションエディターを外す(Claude)

- ユーザー判断「デバッグツールは二個もいらないからいったんF3消して」。
  外部ツール(`tools/MotionEditor.Wpf`)と役割が重複していた。
- [game.cpp](../game.cpp)からF3でMotionEditorSceneへ切り替える分岐を削除した。
  「いったん」なので、シーンのコード(`scene/MotionEditorScene.*`)とプロジェクト登録は残した。
  戻す場合は`game.cpp`へ分岐を足し直すだけでよい。
- 確認: `Debug|x64`ビルドエラー0。

### 2026-09-15 仕上げに向けた方針決定(ユーザー判断)

外部の方からの助言(アニメーション補間/ブレンドツリー/フロムのカメラ/DCCツールのスクリプト/
「ゲームを一番アピールしたいなら、ゲームを作るのに一番効率的なことをする」)を踏まえ、
次の順で進めることにした。

1. **アニメーション(最優先)**
   - 移動のブレンドツリー: 待機↔歩き↔走りを速度で混ぜる。ロックオン中は敵を向いたまま
     横歩き(手持ちのstrafeクリップ4本)を2Dで混ぜる。
   - 攻撃: **足は止めたまま、クリップの踏み込み量をキャラクターの位置へ反映する(ルートモーション)**。
     「攻撃しながら自由に移動する」は採用しない(ユーザー判断)。攻撃を振ったら動けない重さが
     この作品の読み合いの核であり、フロムもモンハンも大半の攻撃は振ると足が止まるため。
     ブレンドツリーは移動とロックオン中の横歩きに使う。
2. **カメラ**: フロムのゲームをもう一段分析して詰める。
3. **質感**: ピクセル単位のライティング、法線マップ、PBR(GGX)。
   プレイヤーのGLBには法線マップ・スペキュラーマップ・金属度/粗さが入っている。ドラゴンは色のみ。

- **DCCツール**: Blenderを使う(ユーザー判断。インストールはユーザーが行う)。
  自作エディタの機能は増やさず、「Blenderで直して保存 → ゲームへ即反映(ホットリロード)」の流れを作る。
- **回復手段**: 保留(2026-09-15 ユーザー判断)。

### 2026-09-15 移動のブレンドツリー(8方向)とロックオン移動(Claude)

- **狙い**: 助言「アニメーション補完・ブレンドツリー」と、フロム作品のロックオン移動。
  以前の移動は歩き/走りの2本を速度のしきい値で即座に切り替えており、
  歩き出し・走り出し・止まる瞬間に姿勢が飛び、ロックオン中も前向きに歩くクリップしか無かった。
- **手持ちクリップの調査**: 使い捨ての調査プログラム(assimpで腰の位置キーを読む)で、
  各クリップの腰が1周期でどちらへ何cm進むかを調べて方向を割り当てた。
  walk=前154cm / walk (2)=後134cm / run=前271cm / run (2)=後219cm /
  strafe (2)=+X132cm / strafe (3)=+X217cm / strafe=-X130cm / strafe (4)=-X222cm。
  Mixamoのキャラクターは+Zを向き、+Xがキャラクターの左。**前後左右 × 歩き/走りの8本が揃っていた**。
  攻撃の slash (5) にも前後46cmの踏み込みがあり、今後のルートモーションに使える。
- **実装**:
  - 新規 [system/LocomotionBlendSpace.h](../system/LocomotionBlendSpace.h)(ヘッダのみ):
    キャラクターから見た速度(右・前)から、待機と8本の重みを求める。
    方向は移動方向を挟む隣り合う2方向を角度で按分、歩調は「待機↔歩き」「歩き↔走り」を速さで按分。
    重みは速度に対して連続なので、切り替わりの瞬間が存在しない。
  - [system/CAnimationMesh.cpp](../system/CAnimationMesh.cpp): `SampleLocalPose()`(クリップの姿勢を
    正規化時間で取り出す。ボーンの状態は変えない)、`ApplyLocalPose()`、`BlendLocalMatrix()`、
    `GetRestLocalMatrix()`、`GetRestBoneModelHeight()`。
  - [system/CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp): `SetLocomotionBlendSpace()` /
    `UpdateLocomotionBlendSpace()`。
    - **位相の同期**: 全移動クリップで1つの正規化時間を共有する。クリップごとに別の時間で進めると、
      混ぜたときに左右の足が揃わない。
    - **足が滑らない再生速度**: 位相を進める速さ = 実際の速さ ÷ 1周期で腰が進む距離。
      1周期の距離は、クリップの腰の移動量をゲーム内の単位へ換算して求める
      (休止姿勢の腰の「親空間の高さ」と「モデル空間の高さ」の比 × 表示倍率)。
      以前の再生速度は目で合わせた固定値(0.33など)だった。モデルを差し替えても換算は自動で合う。
    - 入力の速度へは指数的に追従させる(キーを押した瞬間に重みが跳ばないように)。
    - 待機は上半身だけ(脚は休止姿勢)。待機クリップの脚を使うと宙に浮くことは以前確認済み。
  - [gameobject/player.cpp](../gameobject/player.cpp):
    - **ロックオン中は敵を向いたまま動く**。ダッシュ中は向きの固定を外し、走る方向を向く(エルデンリングと同じ)。
      攻撃中は向き直らない(振っている最中に体が回ると、攻撃の向きが読めなくなる)。
    - **回避は入力した方向へ転がる**。ロックオン中は常に敵を向くので、以前の「向いている方へ転がる」
      ままでは、横へ避けたくても敵へ飛び込んでしまう。入力が無くロックオン中なら敵から離れる方へ下がる。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): 8本のクリップを読み込み、
    実際に動いた量(壁で押し戻された分を含む)からキャラクターから見た速度を求めて渡す。
    クリップは`dev_settings.ini`の`walk_forward` / `run_right` などで差し替えられる。
- **採用理由**: 手持ちのクリップが8方向揃っていたので、Unreal等のブレンドスペースと同じ
  「方向×速さ」の2D補間が組めた。位相同期と、腰の移動量から求める再生速度は、
  混ぜたときの足の破綻と足滑りを防ぐ定石である。
- **不採用案**:
  - 歩き/走りのしきい値切り替えにクロスフェードだけ足す案 — 横・後ろ方向を表現できない。
  - クリップごとに独立した時間で再生する案 — 混ぜると左右の足が揃わない。
  - 腰のルートモーションで移動そのものを駆動する案 — 入力に対する応答が遅くなり、
    アクションゲームの操作感と相性が悪い。移動は入力で決め、アニメーションを合わせる方式にした。
- **踏んだこと**:
  - `CharacterAnimationState`はGameSceneが**並び順で初期化**しているので、新しい項目は末尾へ足す必要があった。
  - キャラクターの右方向の符号を最初逆にしていた。カメラの右方向(cos, sin)と同じ規約にして直した。
  - キャプチャスクリプトが送る疑似キー入力はDirectInput(Tab)に届かない。WASDは`GetAsyncKeyState`でも
    読んでいるので届く。確認用に`dev_settings.ini`の`auto_lockon=1`を追加した。
- **確認**: `Debug|x64`ビルドエラー0。実機キャプチャで、
  - ロックオン中に右へ移動すると、ドラゴンの方を向いたまま右脚を外へ踏み出して回り込むこと。
  - ロックオンなしで止まった状態から前へ歩き出すと、姿勢が飛ばずに左右の足が交互に接地すること。
  - **左・後ろ・走りの各方向は個別には撮っていない**。クリップの左右の割り当てはデータ上の判定なので、
    実際に遊んで左右が逆に見えたら`dev_settings.ini`で入れ替えられる。
- **限界**: 腰(pelvis)は動かしていないので、走りでも腰の上下動が出ない(足のIKが無いと崩れるため)。
  ロックオン中にダッシュすると向きの固定が外れる。回避のモーションは手続き的な前転1種類のまま。

### 2026-09-15 攻撃の踏み込み(ルートモーション)と攻撃中の脚(Claude)

- **狙い**: ユーザー要望「攻撃時踏み込んでほしい。モンハンの片手剣のアニメーションを勉強してほしい」。
  攻撃中は脚を攻撃クリップから外し腰も固定していたため、棒立ちで腕を振るだけで、その場から一歩も動かなかった。
- **モンハンの片手剣の調査**(参考: Game8 / Monster Hunter Wiki の技解説):
  コンボの出だしに**突進斬りで大きく踏み込んで間合いを詰める**、続く縦斬りは小さく前へ、
  横斬り・斬り上げ・回転斬りはその場で腰をひねる、という構成。
  「歩いて近づくより突進斬りで詰めろ」と解説されるほど、踏み込みが立ち回りの一部になっている。
- **手持ちの攻撃クリップの踏み込み量**(腰の位置キーから計測、cm):
  | クリップ | 長さ | 最終的な前進 | 途中の最大前進 | 腰の沈み | 腰のひねり |
  |---|---|---|---|---|---|
  | slash | 1.50s | 0 | 47 | -12 | 88° |
  | slash (2) | 3.53s | 229 | 229 | -40 | 一回転 |
  | slash (3) | 1.67s | 1 | 47 | -2 | 109° |
  | slash (4) | 2.43s | 96 | 96 | -41 | 一回転 |
  | slash (5) | 1.37s | 1 | 46 | 0 | 42° |
  | attack | 2.33s | 340 | 346 | -52 | 86° |
  | attack (2) | 1.30s | 250(+横106) | 250 | -10 | 一回転 |
  | attack (3) | 1.73s | 226 | 226 | -26 | 一回転 |
  | attack (4) | 1.00s | 0 | 7 | -2 | 63° |
  `attack`(約340cm前進)が突進斬りに最も近い。
- **重大な発見**: 攻撃クリップは**元の長さに関係なく0.95秒(弱)へ詰めて再生**されていた
  (戦闘判定の予兆+判定+硬直の合計に合わせているため)。弱2段目の slash (2) は本来3.53秒・229cm前進・一回転の技で、
  3.7倍速かつその場から動かずに再生されていた。モーションがぎこちなく見える大きな原因の1つ。
- **実装**:
  - [system/CAnimationMesh.cpp](../system/CAnimationMesh.cpp): `SampleBonePositionOffset()`(腰の位置の変化量)、
    `SampleHipsInPlace()`(腰をその場に置いたまま沈み込みと回転の変化だけを取り込む)、`DominantAxisComponent()`。
  - [system/CCharacterAnimator.cpp](../system/CCharacterAnimator.cpp): 立ち止まって攻撃するときは、
    **脚と腰の沈み込み・回転も攻撃クリップから取り**、腰の水平移動の差分を`ConsumeRootMotion()`で取り出せるよう溜める。
  - [gameobject/player.cpp](../gameobject/player.cpp): `applyRootMotion()`。次のupdateの中で位置へ足すので、
    壁との衝突補正(更新前後の差分)を通る。敵との押し合いでドラゴンを突き抜けることもない。
  - `dev_settings.ini`の`attack_hips_rotation`(0=回転なし/1=先頭からの変化(既定)/2=クリップのまま)。
- **踏んだ罠(重要)**: **モデルの腰の親空間はZ軸が上、クリップはY軸が上**だった
  (腰の休止位置が(0.03, 0.47, 95.6))。Y成分を高さと決め打ちしたため単位の換算が約200倍になり、
  **踏み込みでプレイヤーが闘技場の外まで飛んだ**(実機キャプチャで発見)。
  腰の休止位置で絶対値が最大の成分を高さとみなすように直した。
  同じ理由で、腰の回転の変化を**親空間で**重ねると回転軸がずれる。**腰自身のローカル空間で**
  (変化 = 現在 × 先頭の逆、結果 = 変化 × 休止姿勢)重ねるように直した。
  以前「腰の回転を入れると寝転ぶ」「沈み込みを入れると埋まる」と記録していた失敗も、
  この軸の違いが原因だった可能性が高い。
- **換算を直した影響**: 前回の移動ブレンドツリーの足合わせも、この誤った換算を使っていた
  (足が元の0.5倍速で滑っていた)。直した結果、**移動速度がキャラクターの体格に対して速すぎる**ことが分かった。
  身長約18単位に対して歩き70単位/秒(人間換算で約7m/秒)。足を合わせるには歩きを約4.5倍速で再生する必要があり、
  見た目が破綻しないよう2.5倍で止めている(その分わずかに足が滑る)。移動速度の見直しはユーザー判断待ち。
- **確認**: `Debug|x64`ビルドエラー0。実機キャプチャで、
  - 弱攻撃1段目(slash (5))で、低く踏み込んで前脚の膝を曲げ後ろ脚を伸ばした構えで斬り、立ち姿勢へ戻ること。
  - `weak1`を`attack`(約340cm)へ一時的に差し替え、離れた位置からドラゴンの足元まで踏み込んで命中し、
    ドラゴンの体で止まること。
  - 歩きの姿勢が換算の修正後も崩れないこと。被弾ののけぞりにも脚の踏ん張りが入ること。
- **限界**: 攻撃クリップは今も0.95秒へ詰めて再生している。足のIKは無いので、急な斜面などは想定していない。

### 2026-09-15 時間ベースの確認 / ロックオンで体を向けない / 足合わせ / コンボの段ごとのクリップと判定(Claude)

#### 1. アニメーションが時間ベースか(fpsベースでないか)の確認
- ユーザー要望: Switchとプレステなどではfpsが違うので、アニメーションは時間ベースにする。
- メインループは可変フレーム(毎フレームの実経過時間を渡し、上限約65fps)。
  「更新1回ごとに一定量進む」処理はfpsによって速さが変わる。
- **確認結果**: プレイヤーのアニメーション・カメラ・戦闘判定・敵AI・エフェクトは経過時間で進んでいた。
  **fpsに依存していたのは2か所**:
  - **敵のアニメーション**: 更新1回ごとに1カウント、10カウントで1キー進めていた(30fpsで半分の速さになる)。
    → `m_enemyAnimationTick`を「次のキーまでの進み具合(0〜1)」にし、経過時間で進めるよう修正。
  - **プレイヤーの振り向き**: 更新1回ごとに差分の40%ずつ回していた。
    → `1 - (1 - 0.4)^(経過秒 × 60)`の指数補間へ修正(60Hzでは従来と同じ見た目)。

#### 2. ロックオンしても体を敵へ向けない
- ユーザー判断: モンハンはロックオン(ターゲットカメラ)でカメラだけが敵を追い、キャラクターは入力方向を向く。
- [scene/GameScene.cpp](../scene/GameScene.cpp)の`LOCK_ON_FACES_TARGET = false`。trueにすればフロム式(体も敵を向く)に戻る。
  回避は入力方向へ転がる変更はそのまま残した。

#### 3. 足運びを今の移動速度へ合わせる
- 歩き/走りの境目を、ゲーム側の移動速度ではなく**クリップを1倍速で再生したときの速度**(歩き約14、走り約39単位/秒)から決めるようにした。
  ゲーム側の速度(歩き70・ダッシュ約115)を境目にすると歩きクリップを約4.5倍速で回すことになり、足がばたばたした。
  今は歩きキーで走りクリップ約1.8倍、ダッシュで約3倍。再生速度の上限を2.5倍→4倍へ広げ、足滑りを無くした。
- 確認: 歩きキーで走りクリップが選ばれ、左右の足が交互に接地することをキャプチャで確認。
  **ダッシュは未確認**(キャプチャスクリプトの疑似入力の左ShiftがDirectInputに届かないため)。

#### 4. コンボの段ごとのクリップと判定(モンハンの片手剣を参考に)
- 以前はクリップを元の長さに関係なく0.95秒(強1.10秒)へ詰めて再生し、判定の時間も全段同じだった。
- [system/CombatAttackTable.h](../system/CombatAttackTable.h)に`PlayerComboStep`と`PlayerComboStepOf()`を追加。
  段ごとに「再生を始める位置・判定が出る/消える位置・終わる位置(すべてクリップの秒)・再生速度・次の段へつなげるまでの時間」を持つ。
  戦闘判定の予兆・判定・硬直はここから計算する。アニメーター(`PlayImportedComboStep()`)も同じ表を見るので、振りと判定がずれない。
- **振っている区間はデータから測った**: 調査プログラムでクリップの右腕(肩・上腕・前腕・手・胸)の回転の速さを求め、
  最大の50%を超える区間を振りとした(slash(5) 0.49-0.64秒、attack 1.05-1.28秒 など)。その前後へ余裕を持たせて判定区間にした。
- **構成**(モンハンの片手剣: 突進斬りで詰める → 小さく斬る → 腰をひねって斬り返す、を手持ちのクリップで再現):
  - 弱: `attack`(跳び上がって約223cm前へ出る斬り下ろし。突進斬りの役) → `slash (5)`(低く踏み込む斬り) → `slash (3)`(腰をひねる斬り返し)
  - 強: `attack (4)`(その場の重い一撃) → `attack (3)`(約226cm前進する回転斬り) → `slash (4)`(沈み込んで振り抜く回転斬り)
- **振り終わった直後から次の段へつなぐ**: 以前は硬直が全部終わってから次の段へ進んでいた。
  クリップ本来の長さで再生すると戻りの動きが長いので、次の段が予約されていれば判定が消えて少し(0.05〜0.10秒)で次へ移る。
- 調整用: `dev_settings.ini`の`enemy_passive=1`(敵が攻撃しない練習用の的)。
- 確認: `Debug|x64`ビルドエラー0。練習用の的に対して弱攻撃を連打し、
  跳び上がって斬り下ろす → 低く踏み込んで斬る → 腰をひねって斬り上げる、の3段が途切れずにつながり、
  1段目へ戻って繰り返すことをキャプチャで確認。敵が攻撃してくる通常の戦闘でも、コンボ中に命中の火花が出ることを確認。
- **限界・未確認**:
  - 強攻撃のコンボは撮影確認していない。
  - 弱1段目が跳躍斬りなので、モンハンの突進斬り(地面を滑るように詰める)とは見た目が違う。
  - 攻撃全体の長さが変わったので、秒間の与ダメージや敵の隙に何回入るかといった戦闘の手触りも変わっている。
  - `weak1`などをiniで別のクリップへ差し替えると、表の振りの位置と合わなくなる。

### 2026-09-15 歩く速さ・弱2段目の足の浮き・ダッシュ攻撃(Claude)

ユーザー指摘:「歩く速度が速すぎる(ゴキブリかと思った)」「弱攻撃の2段目で足が宙に浮いていた」
「1段目の飛び込みは、ダッシュ中の攻撃から派生する方がよい(モンハンにもある)。弱1段目にはふさわしくない」。

#### 歩く速さ
- 歩き70→**20**、ダッシュは歩きの1.65倍→**2.25倍(45)**。身長約18単位に対して歩き70は人間換算で秒速約7mだった。
  歩きクリップの1倍速(約14)の約1.4倍、走りクリップの1倍速(約39)の約1.15倍。
- 移動速度が体格に合ったので、歩き/走りのクリップを切り替える境目をゲーム側の速さ(20 / 45)へ戻した。
  歩いているときは歩きクリップだけ、ダッシュ中は走りクリップだけが再生される。
- 確認: 歩きの足運びが落ち着いたこと(80ms間隔で1歩に2〜3コマ)をキャプチャで確認。

#### 弱2段目で足が宙に浮く
- 原因: slash (5) はクリップの先頭ですでにしゃがんでいる(腰41cm、立つと約92cm)。
  腰の高さを「クリップ先頭からの変化」で足していたので変化が0のまま腰が立った高さに残り、
  しゃがんだ脚の角度で足が浮いた。
- 修正: [system/CAnimationMesh.cpp](../system/CAnimationMesh.cpp)の`SampleHipsInPlace()`で、
  **腰の高さをクリップの値そのまま**(絶対値)にした(クリップとモデルの腰の位置は同じcm単位)。
  腰の回転も同じ理由で、基準を攻撃クリップ自身の先頭ではなく**立ち姿勢の待機クリップの先頭**にした。
- 確認: 弱攻撃(slash (5))を練習用の的に出し、低く踏み込んで斬る間も体が地面の高さにあり足が浮かないことを確認。

#### ダッシュ攻撃(突進斬りの役)
- [system/CombatAttackTable.h](../system/CombatAttackTable.h): `PlayerDashAttackStep()`(attack: 跳び上がって約223cm前へ出る斬り下ろし)。
  弱コンボは slash (5) → slash → slash (3) に組み直した。
- [system/OneVsOneCombat.cpp](../system/OneVsOneCombat.cpp): 走りながら弱攻撃を押すと、1段目がダッシュ攻撃になる
  (`SetPlayerSprinting()` / `IsPlayerDashAttack()`)。そのまま弱攻撃の2段目へつながる。
- [scene/GameScene.cpp](../scene/GameScene.cpp): 前フレームが「ダッシュ」状態なら、アニメーションもダッシュ攻撃を再生する。
  戦闘側とアニメーション側で同じ判定値を使う。`dev_settings.ini`の`dash_attack`でクリップを差し替えられる。
- 確認: 走っている状態で弱攻撃を押すと、跳び上がって斬り下ろすことをキャプチャで確認
  (意図せず走り状態で撮れたもの。後述の入力の不具合を参照)。

#### 見つけて直した不具合: ゲームが前面でなくてもキー入力を拾っていた
- 移動キー(WASD)をDirectInputに加えて`GetAsyncKeyState`でも読んでいたが、
  `GetAsyncKeyState`は**PC全体のキー状態**を返すため、ゲームのウィンドウが前面でなくても反応していた。
  撮影中に入力していないのにプレイヤーが走り出す現象で発覚した。
- 修正: [gameobject/player.cpp](../gameobject/player.cpp)と、ダッシュキー(左Shift)の読み取りで、
  **ゲームのウィンドウが前面にあるときだけ**`GetAsyncKeyState`を使う。

#### 作業中の事故(記録)
- 置換にperlの`s|...|...|`を使い、パターン中の`\|\|`(「||」のつもり)が区切り文字の扱いで**選択(空文字にもマッチ)**になった。
  `GameScene.cpp`の**全2954行の先頭**に同じ2行が挿入され、`player.cpp`の先頭にもブロックが入った。
  挿入された文字列を正確に特定し、その文字列だけを文字どおり取り除いて復元した(未コミットの作業が多く、git checkoutでは戻せなかった)。
  復元後に差分を見て、意図した変更だけが残っていることを確認した。
- 確認: `Debug|x64`ビルドエラー0。

### 2026-09-15 弱コンボの段を見分けられるようにする / 敵なしでも剣を振れる(Claude)

#### 弱攻撃の2段目が1段目と見分けられない
- ユーザー指摘: 「弱攻撃の二段目が一段目と似すぎてどっちかわからん」。
- 調査: 攻撃クリップごとに右手の軌跡を順運動学で求める調査プログラム(`probe5`)を作って比べた。
  slash (5)(1段目)と slash(2段目)はどちらも**剣を同じ側の高い位置へ上げ、斜めに振り下ろす**軌道で、
  高さの変化(約90cm→約30cm)まで同じだった。指摘どおり、見た目で段を区別できない。
- 手持ちのクリップの軌道の種類:
  | 軌道 | クリップ |
  |---|---|
  | 同じ側から斜めに振り下ろす | slash (5) / slash / slash (2)の前半 |
  | 低い位置から斬り上げる | slash (3) / attack (3)(跳躍付き) |
  | 反対側から横へ薙ぐ | attack (4) |
  | 回転斬り | slash (4) / slash (2) / attack (2) |
- 変更([system/CombatAttackTable.h](../system/CombatAttackTable.h)):
  - 弱: slash (5) 振り下ろし → **slash (3) 斬り上げ** → **attack (4) 横薙ぎ**。段ごとに剣の向きを変えた。
  - 強の1段目: attack (4) を弱へ回したので **slash** にした。高く振りかぶって約42cm踏み込みながら振り下ろす。
    溜めを0.36秒と長めにして、弱攻撃より重く見せる。
  - ダッシュ攻撃(attack)と強の2・3段目は変更なし。
- kick(蹴り)は見た目は一番違うが、攻撃判定が剣の刃なので当たらない。使っていない。
- 確認: 敵なしで弱攻撃を3回入力し、振り下ろし → 真上への斬り上げ → 反対側からの横薙ぎと、
  剣の軌跡がそれぞれ別の向きに出ることをキャプチャで確認。カメラを一時的に引いて、3段とも足が影と接している(浮いていない)ことも確認。
  強攻撃1段目の振りかぶりと振り下ろしも確認。

#### 敵がいないと剣を振れない
- 原因: プレイヤーの攻撃の段・判定・硬直の進行は`OneVsOneCombat::Update()`の中にあり、
  この関数は敵がいるときしか呼ばれていなかった。敵を消すと、クリックしても1段目のモーションが出るだけで攻撃の状態が進まず、
  コンボもつながらなかった。
- 修正:
  - [system/OneVsOneCombat.cpp](../system/OneVsOneCombat.cpp): プレイヤーの攻撃の処理を`AdvancePlayerAttack()`へ切り出し、
    敵がいるとき(`Update()`)といないとき(`UpdateWithoutEnemy()`)で同じ処理を通す。敵がいないときはダメージを出さない。
  - [scene/GameScene.cpp](../scene/GameScene.cpp): 敵がいないときも`UpdateWithoutEnemy()`を呼ぶ。
    コンボの段が進んだときにモーションを切り替える処理を、敵の有無の分岐の外へ出した。
  - 素振りの入口: デバッグ表示の「敵」タブに**「敵を全員消す(素振り)」**ボタン。
    起動時から敵なしにしたい場合は`dev_settings.ini`に`no_enemy=1`(登場演出も飛ばしてすぐ操作できる)。
  - 敵がいない状態で登場演出が始まったとき(Rで仕切り直し、演出中に敵を消す)は、BGMを流して戦闘カメラへ戻す。
- 確認: `no_enemy=1`で起動し、弱コンボ3段と強攻撃が出ることをキャプチャで確認。`Debug|x64` / `Release|x64`ビルドエラー0。
- 未確認: 敵なしでのダッシュ攻撃(撮影ツールから左Shiftの入力を入れられないため)。→ 次の項で確認した。

### 2026-09-15 踏み込みが後ろ向きだった / 強攻撃2段目が振り切る前に3段目へ移る(Claude)

#### 走っている向きとダッシュ攻撃の向きが違う
- ユーザー指摘: 「走ってる向きとダッシュattackの向きが違う」。
- 調査: 走りながらダッシュ攻撃を出し、プレイヤーの位置と向きを毎フレームファイルへ書き出した(調査後に削除)。
  **走りは-Z(正面)へ進んでいたのに、ダッシュ攻撃が始まると+Zへ約25単位戻っていた**。体は正面を向いたまま後ろへ動くので、向きが食い違って見えた。
  クリップ自体の腰の移動はほぼ真っすぐ(横ずれは340cmに対して7cm未満)で、クリップのせいではなかった。
- 原因: モーションは`aiProcess_ConvertToLeftHanded`付きで読み込んでおり(`system/CAnimationData.cpp` / `AssimpPerse.cpp`)、
  **Z軸が反転してクリップの前は-Zになる**。踏み込みの取り出し(`CCharacterAnimator.cpp`)は+Zを前として足していた。
  調査プログラムは読み込みフラグ無しでクリップを読んでいたので、前が+Zに見えていた。
- 影響: **踏み込む攻撃はすべて後ろへ下がっていた**(ダッシュ攻撃、強2段目の attack (3) など)。
  以前「踏み込みでドラゴンの足元まで詰めて命中した」と記録したが、この符号では前へは出ないので、その記録は誤りだった可能性が高い。
- 修正: 前後の移動量の符号を反転(`-(current.z - previous.z)`)。左右(右が-X)は反転の影響を受けないのでそのまま。
- 確認: 同じ位置ログで、ダッシュ攻撃が走りと同じ-Zへ約25単位進むこと(横ずれ1単位未満)。走り45単位/秒からダッシュ攻撃の約35単位/秒へ自然につながる。
  キャプチャでも、背中を見せたまま前へ跳んで斬ること。

#### 強攻撃の2段目が振り切る前に3段目へ移る
- ユーザー指摘: 「強攻撃の二段目が振り切る前に次の三段目に行く」。
- 調査: 右手のワールド空間での速さを0.05秒ごとに測った(`probe6`)。attack (3) は回転しながら2回振るクリップで、
  2回目の振りは0.75〜1.05秒(最速は0.95秒)。判定の終わりを0.90秒にしていたので、次の段へは1.0秒で移り、振りの途中で切れていた。
  振りの区間を**右腕の関節の回転速度**だけで測っていたため、体の回転で剣が運ばれる分を見落としていた。
- 修正: 強2段目を`{0.34, 0.62, 1.05, 1.60, 1.25, 0.12}`に(次の段へ移るのはクリップの約1.2秒)。
- 他の段も手の速さで確かめ、次の段へ移る位置が振りの後になっていることを確認した
  (slash (5) 0.77秒 / slash (3) 1.02秒 / slash 0.81秒 / attack 1.43秒。いずれも手の速さが落ちた後)。
- 確認: 強攻撃を3回入力し、2段目が剣を振り上げきってから3段目の振りかぶりが始まることをキャプチャで確認。
  `Debug|x64` / `Release|x64`ビルドエラー0。

### 2026-09-15 ダッシュ攻撃で体が斜めを向く / 作業フォルダの分離(Claude)

#### 前を向いているのに斜めを向いて攻撃する
- ユーザー指摘: 「ダッシュ攻撃まだ治ってない。前向いてるのに斜め向いて攻撃してる」(進む向きは前の修正で直っていた)。
- 調査: ゲームと同じ読み込みフラグでクリップを読み、腰の向きを測った(`probe7`)。
  - どのクリップも、ファイル自身のバインド姿勢の腰の回転は**0(単位回転)**。
  - 待機クリップの先頭は、片手剣の構えで**腰が55度ひねれている**。
  - ダッシュ攻撃(attack)の跳躍中の腰は0〜27度で、ほぼ正面。
- 原因: `SampleHipsInPlace()`が攻撃中の腰の回転を「待機クリップの先頭」を基準に求めていたため、
  攻撃中の体が常に約50度斜めを向いていた。その場の攻撃では目立たないが、前へ跳ぶダッシュ攻撃で進む向きとずれて見えた。
- 修正([system/CAnimationMesh.cpp](../system/CAnimationMesh.cpp)): 基準を**クリップのバインド姿勢(単位回転)**にした
  (`回転 = 現在 * 休止姿勢`)。先頭で前かがみのクリップの傾きも残る。基準クリップの引数は削除した。
- 確認: 走りからダッシュ攻撃を出し、背中をまっすぐ見せたまま前へ跳んで斬ることをキャプチャで確認。
  弱コンボの3段が見分けられ、足が接地していることも確認。`Debug|x64` / `Release|x64`ビルドエラー0。

#### 作業フォルダの分離(重要)
- ユーザーの方針: **Codexは`feature/hata-visual-polish`(見た目)、Claudeは`feature/hata-player-hp-stamina-hud`**で作業する。
- 経緯: 同じフォルダでブランチが`feature/hata-visual-polish`へ切り替わったとき、未コミットの作業(19ファイル)が作業フォルダから消えた。
  切り替え直前にgitへ書き込まれていたツリー(`16f8389b`)に全ファイルが残っていたので、そこから復元した。
- 対応: 同じフォルダで2つのブランチを使うと互いのファイルを入れ替えてしまうため、
  **Claude用に`git worktree`で別フォルダ`C:\Users\yu04k\Desktop\git\DX11_3D_Game_claude`を作り**、そこで元のブランチの作業を続ける。
  git管理外の`third_party/DirectXTK/lib`、`third_party/assimp/bin`、`assets/model/CethielDragon/dragon.obj`、`dev_settings.ini`はコピーした。
- このフォルダでは`DX11_3D_Game.sln`のビルドがWPFのモーションエディタ(NuGet未復元)で失敗するので、
  ゲーム本体の`DX11_3D_Game\DX11_3D_Game.vcxproj`を`/p:SolutionDir=<フォルダ>\`付きでビルドする。
